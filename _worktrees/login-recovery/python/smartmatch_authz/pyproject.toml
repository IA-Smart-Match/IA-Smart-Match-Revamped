[project]
name = "smartmatch-authz"
version = "0.1.0"
description = "Deny-by-default SmartMatch authorization policy — pure, no frameworks"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["smartmatch_authz"]
