# Class exercise — open-question register

**Status:** scoped register for `ProductScope.CLASS_EXERCISE`, created
2026-09-16. Rows here concern the class exercise only. Nothing here closes an
OQ-SC, OQ-SE, or OQ-CBA row, and nothing in those registers closes a row here:
the exercise stores no real student and no real event, so the privacy, records,
and consent questions of the CBA track do not arise in it.

**Owner of the scope:** Ann Wang (instructor and project lead). Placeholders
below are the numbers in her build table and stay in force until she confirms
or replaces them.

| ID | Decision question | Owner | Placeholder / safe default | Blocks | Status |
|---|---|---|---|---|---|
| OQ-CE-01 | What are the final column names and value vocabularies of the data file (major, year, past events, stated interests, career goal, hidden true interests)? | Ann | **Answered by Ann's data file Read Me, 2026-09-24**, and her cover email the same day ("The column names are final, and the 'Read Me' sheet explains each column"). Two sheets, `Profiles` (P001–P300) and `Events` (E01–E12); 6 majors, 4 years (seniors first), one fixed list of 13 topics, 16 career-goal labels; `;` separates entries in a cell; past events are named by id; `card_completed` says whether a card exists; `tiebreak_order` is the fixed order; two hidden columns (`hidden_true_interests`, `hidden_true_career_goal`). Owner rulings of 2026-09-24 close the vocabularies in code (`smartmatch_domain/exercise/vocabulary.py`) and set the year order Senior > Junior > Sophomore > Freshman. Implemented in PR #228. | Ingest, factors, markers | **CLOSED 2026-09-24** — Ann's data file Read Me, 2026-09-24; owner rulings 2026-09-24 |
| OQ-CE-02 | What default weights do the four factors carry when a team opens an event? | Ann + Chau | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** "Equal is fine. Teams should decide for themselves which factors matter most." Equal weights (0.25 each) stay as the four named constants in `smartmatch_domain/exercise/registry.py`; the `PLACEHOLDER (OQ-CE-02)` markers are removed and the values are unchanged. | Matching | **CLOSED 2026-09-25** — Ann Wang, email reply |
| OQ-CE-03 | What are the four coefficients of the simulated-results rule (true-interest fit lift, frequent-attender lift, same-major lift, chance size)? | Ann + Chau | **Ann's qualitative answer, 2026-09-25 (email reply to the team's question list):** the event matches what the student genuinely cares about — **a lot**; the student has attended events before — **some**; the event is targeted toward the student's major — **a little**; random chance — **some randomness**. She was told Chau would translate those into exact numbers and show her a sample result before class. The team's translation ships as `EXERCISE_SIMULATION_COEFFICIENTS` in `smartmatch_domain/exercise/simulation.py`, marked `PLACEHOLDER (OQ-CE-03: …)`: start 4 in 100; true fit 40 (split evenly between true interests and career goal); attended at least one past event 10; same major 4; chance up to 10 either way; 75 in 100 of sign-ups attend. Sample result for Chau: [`oq-ce-03-sample-result.md`](oq-ce-03-sample-result.md). | Results | OPEN — Ann's words recorded 2026-09-25; numbers await Chau and Ann's confirmation from the sample result |
| OQ-CE-04 | What are the three "asking for more" percentages? | Ann | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** "All good": 30 percent / 55 percent / 80 percent, plus 15 percent non-responding under "required", confirmed as the named constants in `smartmatch_domain/exercise/asking.py`. Ann did not address halves: half-rounding settled by Danny per the owner-doc recommendation (round half up) — `select_share` rounds `share × n` half up in decimal arithmetic. | Refresh | **CLOSED 2026-09-25** — Ann Wang, email reply; half-rounding Danny (owner) |
| OQ-CE-05 | Is the data file CSV or XLSX? | Ann + Danny | **XLSX** — Ann's file is a workbook (Ann's data file Read Me, 2026-09-24). Owner ruling 2026-09-24 (Danny): the instructor uploads Ann's `.xlsx` as-is; `openpyxl` (read_only, data_only) and `defusedxml` are runtime dependencies of `smartmatch-domain`; the CSV path is replaced, not kept beside it; every earlier guard stays and a zip-bomb guard reads the ZIP central directory before openpyxl opens the file; the body stays raw bytes (no multipart, ruling of 2026-09-21). Implemented in PR #228 (`smartmatch_domain/exercise/workbook.py`). | Ingest | **CLOSED 2026-09-24** — Ann's data file Read Me, 2026-09-24; owner ruling 2026-09-24 |
| OQ-CE-06 | Where does the site live and what is its stable address? And what rate-limits the no-login routes? | Danny | **Decided 2026-09-21, Danny (row owner): the exercise runs at `exercise.plated.blog`, a second hostname on the existing tunnel, exercise scope only, no Access policy; instructor routes protected by the passcode plus proxy rate limiting.** That settles the address and the hosting shape. It does not settle the second half: the proxy rate-limit rule is **not applied yet**, so `services/api/smartmatch_api/exercise_rate_limit.py` stays a marked `PLACEHOLDER (OQ-CE-06)`, per process and unable to bound CPU, until it is. | Hosting | OPEN — address and hosting shape decided 2026-09-21. The row stays OPEN, not CLOSED, because the rate-limit half it also carries (design spec §14; `exercise_rate_limit.py`, `routers/exercise_workspace.py`, `routers/exercise_instructor.py`, `routers/exercise_matching.py`, `routers/exercise_results.py`) is named work nobody has done. Closing it now would mark six marked placeholders as settled. |
| OQ-CE-07 | How is the instructor passcode set and shared with Ann and Dr. Lin? | Danny + Ann | **Decided 2026-09-25, Danny (owner), recording Ann's answers to the team's question list (2026-09-22).** `SMARTMATCH_EXERCISE_INSTRUCTOR_PASSCODE` is set per deployment, shared out of band, and rotated by changing the env value. `.env.example` keeps it commented out and empty. | Instructor page | **CLOSED 2026-09-25** — Danny (owner), recording Ann's answers (2026-09-22) |
| OQ-CE-08 | Do two browser tabs that enter the same team number share one workspace, or is each tab its own workspace? | Ann | **Decided 2026-09-25, Danny (owner), recording Ann's answers to the team's question list (2026-09-22).** A shared workspace per team number, as built (`smartmatch_domain/exercise/workspace_token.py`). | Team workspaces | **CLOSED 2026-09-25** — Danny (owner), recording Ann's answers (2026-09-22) |
| OQ-CE-09 | What license line goes on the opening screen? | Ann | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** The license line is: "For California State Polytechnic University, Pomona — College of Business Administration instructional use only. All student profiles are fictional." Rendered on the opening screen from `EXERCISE_LICENSE_LINE` in `apps/web/legacy-frontend/src/app/pages/exercise/ExerciseEntry.tsx`. | Opening screen | **CLOSED 2026-09-25** — Ann Wang, email reply |
| OQ-CE-10 | How many points does a profile earn per attended event, and for completing a card? | Ann + Chau | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** "All good": one point per attended event and one for completing a card, as the named constants in `python/smartmatch_domain/smartmatch_domain/exercise_points.py`; the `PLACEHOLDER (OQ-CE-10)` markers are removed and the values are unchanged. | Points | **CLOSED 2026-09-25** — Ann Wang, email reply |
| OQ-CE-11 | What are the five questions on the "five quick questions" card? The requirements name only stated interests and career goal as what a completed card holds. | Ann | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** "Please ask only for interests and career goal. Major and year are already on file, and past events are recorded by the app when students attend, so students should not have to enter them again. On activation, students just confirm their major." The card mock-up asks exactly two questions (stated interests, career goal) and shows the major on file with a confirm step; year and past events are not asked. The hidden true interests are never asked. The screen stays labelled a mock-up. | Profile-card mock-up | **CLOSED 2026-09-25** — Ann Wang, email reply |
| OQ-CE-12 | Reason-line wording and precedence (design spec §4.5). Three tie sentences Ann did not write, kept in `reasons.py` until 2026-09-25 as `PLACEHOLDER (wording pending Ann; see PR #180)`: `tied on what counted; ordered by year` (a tie that is not on the major), `tied; more information on file first`, and `tied; placed in a fixed order that never changes` — what are her words for each? Separately, her two verbatim phrases are table cells without a capital or a full stop, so `assert_one_sentence` refuses them as written; they are stored byte for byte as `ANN_MAJOR_ONLY_PHRASE` (`same major; nothing else on file`) and `ANN_TIED_ON_YEAR_PHRASE` (`tied on major; ordered by year`) and rendered capitalised and full-stopped (`Same major; nothing else on file.`), and the single-factor line is framed `What counted: same major.` because the bare label is a two-word fragment — is each rendering acceptable? And precedence: a major-only profile whose place the year decided matches both of her lines, and the requirements rank neither — which line should a class participant see? | Ann | **Decided 2026-09-25, Ann Wang, email reply to the team's question list.** "All good": every reason line approved as written — Ann's two phrases rendered "Same major; nothing else on file." and "Tied on major; ordered by year."; the three drafted sentences "Tied on what counted; ordered by year.", "Tied; more information on file first." and "Tied; placed in a fixed order that never changes."; and "What counted: same major." Precedence: `TIE_LINE_WINS_OVER_MAJOR_ONLY_LINE = True` stays — the tie line wins when both fit. The `PLACEHOLDER` markers in `reasons.py` are removed; no wording changed. | Reason lines | **CLOSED 2026-09-25** — Ann Wang, email reply |
| OQ-CE-13 | When a refresh copies a card onto a profile, what career goal does the copied card carry — the one already on the base row, or nothing? | Ann | **Answered by Ann's data file Read Me, 2026-09-24**: the hidden columns are "used only by the results rule and by the refresh (a new card copies these)", and `hidden_true_career_goal` follows "the same rules as hidden_true_interests". Her cover email says the same of both pink columns. A copied card therefore carries the profile's **hidden true career goal** (`NULL` when the file has none). `COPIED_CARD_CAREER_GOAL = CopiedCardCareerGoal.HIDDEN_GOAL`; the 2026-09-21 `BASE_GOAL` reading and PR #190's `NONE` stay as members. Implemented in PR #228. | Refresh | **CLOSED 2026-09-24** — Ann's data file Read Me, 2026-09-24 |
| OQ-CE-14 | Is the role→topic table right? "Career goal fits this event" compares the topic a goal label points at: each "<field> role" label → its field; `Start my own business` → Entrepreneurship / startups; `Undecided` and `Graduate school` → no topic (a measured miss, not unknown). | Ann | **Answered by Ann, 2026-09-25**: "Own business → startup / entrepreneurship events: yes." "Graduate school → no specific event topic: yes." "Undecided → should match broad exploratory events (company talks, industry panels, career fairs), at half credit, so a student whose goal clearly fits still ranks higher. Treat both Northline and Harbor as exploratory, since they are company events. The results step should treat undecided students the same way." Implemented: `CAREER_GOAL_TOPICS` unchanged and no longer a placeholder; `Undecided` scores 0.5 on "career goal fits this event" for an exploratory event (`UNDECIDED_EXPLORATORY_GOAL_FIT`), and the results rule gives it the same half. Exploratory is read at ingest from `event_type` (Career fair, Employer info session, Industry panel, Employer talk; not Workshop, Competition, Networking) into `exercise_event.is_exploratory` (migration `0043`); Northline and Harbor come out exploratory. | Matching, results rule | **CLOSED 2026-09-25** — Decided 2026-09-25, Ann Wang, email reply to the team's question list. |
| OQ-CE-15 | Ann's Read Me test case says P004 should rank above a plain Accounting major for Northline with "said they are interested" turned up, **and not** with it turned off. Turned off, the factor stops counting for P004, but P004 still ranks above: its goal "Data, analytics or IT role" fits Northline (OQ-CE-14), and with that factor off too, design spec §4.4 puts more information on file first. Which should change, if any — her expectation, the goal table, or the tie-break? | Ann | Built as the spec says and pinned by `tests/golden/exercise/test_exercise_ann_dataset_golden.py` (PR #228), which asserts the factor's contribution turns off and records why the position does not. | Matching | OPEN — ask Ann |

## Closure discipline

A row closes with a dated line naming who decided and what. Ann's Friday
check-in notes are sufficient evidence for this register.

A dated decision on part of a row is recorded in the row's decision cell and
leaves the row OPEN while the rest of it is undone — see OQ-CE-06.

## What is being asked of Ann and Chau

[`class-exercise-proposal-for-ann-and-chau.md`](class-exercise-proposal-for-ann-and-chau.md)
collects the rows above into one short document the owner brings to them. It
closes nothing: every row stays as this register records it until a dated
answer is written here.
