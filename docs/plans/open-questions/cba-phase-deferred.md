# CBA phase deferral and decision register

**Status:** Active planning gate  
**Customer source:** [`docs/product/cba-smart-match-customer-requirements.md`](../../product/cba-smart-match-customer-requirements.md) §§20, 21, 25, 26  
**Applies to:** The CPP CBA pivot only. It does not delete prior IA-West work or authorize live providers.

This register is the fail-closed boundary for the CBA phase. Unresolved product choices are recorded here instead of becoming permanent implementation defaults. Gated capabilities remain in the repository but are not mounted, routed, advertised, or presented as successful on CBA paths.

## Open questions

| ID | Decision required | Safe planning default | Owner and deadline | Unlocks |
|---|---|---|---|---|
| OQ-CBA-001 | How should a virtual event's 30% Proximity weight be redistributed across Industry, Role, and Topic? | Do not ship virtual-event scoring. Proportional weights (42.86/35.71/21.43) may appear only as a labeled proposal in tests or an ADR draft, never as an approved runtime default. | CBA product owner; decide before `CBA-PROXIMITY` merges in Wave 3. | Virtual-event match runs and golden cases. |
| OQ-CBA-002 | What exact numeric score should each 0–25, 25–75, and 75+ mile band receive, including boundary ownership at exactly 25 and 75 miles? | Keep the bands configurable and fail closed for production scoring until values and inclusive/exclusive boundaries are approved. | CBA product owner with matching lead; decide before `CBA-PROXIMITY` golden approval. | Physical-event proximity scoring. |
| OQ-CBA-003 | What rating scale, fields, edit policy, anonymity, and aggregation rules should student speaker feedback use? | Implement no storage or form contract yet. Wave 4 may land only a decision-ready schema proposal and authorization tests; no fake local-state rating UI. | CBA product owner and privacy owner; decide before `CBA-STUDENT-RATING` schema/API work. | Student rating submission and Connector review. |
| OQ-CBA-004 | Does CBA scoring amend ADR-0011 for a customer-directed neutral Topic score and virtual-event weight redistribution, or use an explicitly labeled CBA scoring-mode policy? | Preserve `unknown` as `None`; do not coerce missing evidence to a number and do not re-spread weights in production. Write and approve an ADR amendment or a new CBA scoring ADR before changing the registry. | **Owner: Danny Tran, Development Lead/program owner of record, with the matching-domain reviewer. Decision is a hard pre-merge gate for the first Wave 3 registry PR.** | `REGISTRY_VERSION` 2.x, neutral Topic behavior, virtual redistribution, new golden cases. |
| OQ-CBA-005 | Should ranked candidates expose any overall score in the UI? | Rank internally; show factor-level provenance and Topic reasoning; do not show a prominent overall percentage. | CBA product owner; decide only if a later UI asks to expose the aggregate. | Optional match-result presentation change; not ranking itself. |
| OQ-CBA-006 | Which named capability, if any, owns the `jobs`, `redrive`, `engagement`, and `review` routers? `CBA-SCOPE-POLICY` classified them as capability-neutral infrastructure — the durable command/job substrate every capability rides on — and mounts them unconditionally. | Keep them unconditional. There is no version of this product that offers match runs or imports but not the job lifecycle that carries them, so gating them would break a preserved capability rather than gate an out-of-scope one. Do not invent a capability name for them merely to make the table look complete. | CBA product owner with the API owner; decide only if a later track needs one of these surfaces gated independently. | Any future per-surface gating of the command/job substrate. |
| OQ-CBA-007 | Does the worker (`services/worker/smartmatch_worker/`) need to read the same product-scope policy, and if so which worker behavior changes with it? | The worker reads no scope today and `CBA-SCOPE-POLICY` added none: no current worker command behaves differently per product, and a scope value the worker ignores is worse than no scope value at all. `smartmatch_domain.product_scope` is already importable from the worker if a later track needs it. The prohibition on new CBA `member_inquiry` writers and on live crawl/paid-extraction execution stays enforced where it already is, not by a new worker flag. | CBA product owner with the worker owner; decide before any Wave 5 track proposes a scope-dependent worker command. | Scope-aware worker command routing, if it is ever needed. |

### OQ-CBA-004 decision record requirements

The decision must cite [`docs/architecture/decisions/ADR-0011-accountable-numbers.md`](../../architecture/decisions/ADR-0011-accountable-numbers.md) and answer both cases separately:

1. Missing Topic evidence: whether “neutral” is an explicit customer policy value with distinct provenance or still an unknown value excluded from scoring.
2. Virtual events: whether excluding Proximity is a conditional factor registry, an approved normalization rule, or another versioned policy.

The approved artifact must define serialization, UI labeling, match-run pins/fingerprints, and golden cases. Approval must precede edits to [`python/smartmatch_domain/smartmatch_domain/factor_registry.py`](../../../python/smartmatch_domain/smartmatch_domain/factor_registry.py).

## CBA-gated capabilities

| Capability | CBA disposition | Current implementation surfaces | Gate/re-entry condition |
|---|---|---|---|
| Rewards and points refinements | **Preserve truthful, server-backed rewards/points capability.** Customer §4 explicitly says “Rewards / points — Keep”; §25 makes only refinements and possible career-readiness wording P2. Gate a route, control, or claim only when current evidence shows that specific surface is chapter-specific, unfunded, incomplete, or fake-success. Do not blanket-hide the rewards API or Student Rewards page merely because chapter membership is removed. | [`services/api/smartmatch_api/routers/rewards.py`](../../../services/api/smartmatch_api/routers/rewards.py), [`python/smartmatch_domain/smartmatch_domain/rewards.py`](../../../python/smartmatch_domain/smartmatch_domain/rewards.py), [`python/smartmatch_persistence/smartmatch_persistence/rewards.py`](../../../python/smartmatch_persistence/smartmatch_persistence/rewards.py), [`apps/web/legacy-frontend/src/app/pages/student/StudentRewards.tsx`](../../../apps/web/legacy-frontend/src/app/pages/student/StudentRewards.tsx), [`docs/decisions/d6-rewards-budget-decision-record.md`](../../decisions/d6-rewards-budget-decision-record.md) | P2 may refine CBA wording/career-readiness framing. Any narrow gate must name the defective surface and its re-entry condition; working ledger-backed catalog, balance, and redemption behavior remain in scope. |
| `member_inquiry` pipeline stage | Preserve schema/history and R/Y/G discovery feed; suppress the CBA funnel tile and prohibit new CBA writers. | [`python/smartmatch_domain/smartmatch_domain/pipeline.py`](../../../python/smartmatch_domain/smartmatch_domain/pipeline.py), [`python/smartmatch_persistence/smartmatch_persistence/pipeline.py`](../../../python/smartmatch_persistence/smartmatch_persistence/pipeline.py), [`db/migrations/versions/0011_pipeline_record.py`](../../../db/migrations/versions/0011_pipeline_record.py), [`apps/web/legacy-frontend/src/app/components/PipelineFunnelTiles.tsx`](../../../apps/web/legacy-frontend/src/app/components/PipelineFunnelTiles.tsx) | Re-enter only with a CBA-defined post-event outcome and metric-register definition. Wave 5 may implement `confirmed` and `attended`, not `member_inquiry`. |
| Legacy cold outreach and specialist discovery | Do not mount, navigate to, or reuse on CBA paths. Preserve only the consented `/v1` outreach path. | [`apps/web/legacy-frontend/src/app/pages/Outreach.tsx`](../../../apps/web/legacy-frontend/src/app/pages/Outreach.tsx), [`apps/web/legacy-frontend/src/components/AgenticOutreachPanel.tsx`](../../../apps/web/legacy-frontend/src/components/AgenticOutreachPanel.tsx), legacy helpers in [`apps/web/legacy-frontend/src/lib/api.ts`](../../../apps/web/legacy-frontend/src/lib/api.ts); preserved path in [`services/api/smartmatch_api/routers/outreach.py`](../../../services/api/smartmatch_api/routers/outreach.py) and [`services/worker/smartmatch_worker/outreach.py`](../../../services/worker/smartmatch_worker/outreach.py) | Re-entry requires a future customer scope change. Consent eligibility in [`python/smartmatch_domain/smartmatch_domain/consent.py`](../../../python/smartmatch_domain/smartmatch_domain/consent.py) must never be weakened. |
| External event/speaker discovery, crawling, scraping, and paid extraction | Disable live execution and hide crawl controls. Fixture ingest remains usable for tests only; CBA records grow manually. | [`docs/decisions/g3-crawler-decision.md`](../../decisions/g3-crawler-decision.md), [`python/smartmatch_providers/smartmatch_providers/fixture_ingest.py`](../../../python/smartmatch_providers/smartmatch_providers/fixture_ingest.py), [`services/worker/smartmatch_worker/paid_extraction.py`](../../../services/worker/smartmatch_worker/paid_extraction.py), [`apps/web/legacy-frontend/src/components/CrawlerFeed.tsx`](../../../apps/web/legacy-frontend/src/components/CrawlerFeed.tsx) | Re-entry requires explicit customer authorization, signed threat model, and live-data/provider gates. Customer §20 currently prohibits it. |
| Landing-page scraping/CRM narrative | Replace or hide CBA-inaccurate claims; do not imply external acquisition. | [`apps/web/legacy-frontend/src/app/pages/LandingPage.tsx`](../../../apps/web/legacy-frontend/src/app/pages/LandingPage.tsx) | Re-enter copy only if it truthfully describes in-scope manual records and consented workflows. |
| CPP green/gold redesign | Defer. A small token-only change may be separately approved, but branding cannot block functionality. | [`apps/web/DESIGN.md`](../../../apps/web/DESIGN.md), legacy frontend theme assets | Re-enter after functional demo path and design Part 2 decisions; customer §21/P2. |
| Live Resend email | Keep fixture transport and fail-closed selection. | [`services/worker/smartmatch_worker/outreach.py`](../../../services/worker/smartmatch_worker/outreach.py), provider configuration referenced by [`services/api/smartmatch_api/config.py`](../../../services/api/smartmatch_api/config.py) | Explicit provider credentials, non-classroom edition, approved templates/domain, and user authorization. |
| Live JWKS/institutional identity | Preserve the single pilot login; do not add role selection or claim institutional SSO. | [`services/api/smartmatch_api/routers/auth.py`](../../../services/api/smartmatch_api/routers/auth.py), [`docs/decisions/pilot-login-decision-2026-09-04.md`](../../decisions/pilot-login-decision-2026-09-04.md) | A1b institutional configuration and explicit live-identity approval. |
| Terraform/cloud apply | Plan/validate only; no apply and no cloud-ready claim. | [`infra/terraform/`](../../../infra/terraform/), [`tools/env_isolation_check.py`](../../../tools/env_isolation_check.py) | Explicit user authorization with deployment target, credentials, and live gates. |

## Superseded IA-West execution goals

- Do not re-run G2, G3, or G4; their shipped work remains historical architecture, not CBA scope.
- G1 may continue only for `confirmed` and `attended`; no CBA `member_inquiry` writer.
- G5 and G6 live integrations remain deferred.
- `P-CRAWL-LIVE` remains gated off. Do not re-run superseded IA-West `P-REWARDS-*` goals as written; this is not a gate on the working rewards capability. A future CBA rewards card must preserve server-backed behavior and limit itself to approved P2 wording/refinements or a named truthfulness defect.
- G7, G8, and G9 are eligible for Wave 5 only when rewritten with the CBA fences in this register.

## Enforcement expectations for Wave 0

Planning assumes one explicit `PRODUCT_PHASE=cba`-style capability policy (exact representation to be chosen during implementation) shared by API composition and frontend navigation. Tests must prove gated CBA routes/links are absent or return a truthful disabled response, while preserved consented outreach, event reads, discovery feed, and one backend-derived login flow remain available.

`ALLOW_LIVE_PROVIDERS=false`, `ALLOW_LIVE_DATA=false`, and `ALLOW_CLOUD_DEPLOY=false` remain mandatory defaults. This document authorizes no code or environment change.
