# CBA phase deferral and decision register

**Status:** Active planning gate  
**Customer source:** [`docs/product/cba-smart-match-customer-requirements.md`](../../product/cba-smart-match-customer-requirements.md) §§20, 21, 25, 26  
**Applies to:** The CPP CBA pivot only. It does not delete prior IA-West work or authorize live providers.

This register is the fail-closed boundary for the CBA phase. Unresolved product choices are recorded here instead of becoming permanent implementation defaults. Gated capabilities remain in the repository but are not mounted, routed, advertised, or presented as successful on CBA paths.

## Open questions

| ID | Decision required | Safe planning default | Owner and deadline | Unlocks |
|---|---|---|---|---|
| OQ-CBA-001 | How should a virtual event's 30% Proximity weight be redistributed across Industry, Role, and Topic? | **Closed 5 September 2026 by ADR-0016 (Accepted).** Virtual events drop Proximity via a named pinned scoring mode (`cba-virtual-1`), not a second registry, and the remaining 30% is redistributed by proportional renormalization to Industry `0.428571`, Role `0.357143`, Topic `0.214286` — matching customer §11's "approximately 42.86 / 35.71 / 21.43". See ADR-0016 Proposals 5 and 6. | CBA product owner; decide before `CBA-PROXIMITY` merges in Wave 3. | Virtual-event match runs and golden cases. |
| OQ-CBA-002 | What exact numeric score should each 0–25, 25–75, and 75+ mile band receive, including boundary ownership at exactly 25 and 75 miles? | **Closed 5 September 2026 by ADR-0016 (Accepted).** Proximity is a step function on raw miles from CPP campus with no interpolation inside a band: Near `0 ≤ d < 25` → `1.00`, Mid `25 ≤ d < 75` → `0.60`, Far `75 ≤ d` → `0.20`. Boundaries are lower-inclusive and upper-exclusive throughout, so exactly 25 miles is Mid and exactly 75 miles is Far, compared against the raw float with no pre-rounding. Far is `0.20` rather than `0.00` so a measured zero stays a real claim. A speaker with no city and no ZIP is `unknown`, explicitly **not** the Far band. See ADR-0016 Proposals 3, 4 and 5. | CBA product owner with matching lead; decide before `CBA-PROXIMITY` golden approval. | Physical-event proximity scoring. |
| OQ-CBA-003 | What rating scale, fields, edit policy, anonymity, and aggregation rules should student speaker feedback use? | **Closed 6 September 2026 by the decision recorded below.** One required overall integer rating from 1 through 5 and one optional comment; one attributed row per student/event/speaker, editable or withdrawable until seven days after the event; withdrawal retains the row and its attribution but clears the rating and comment. Connector reads are aggregate-only: mean and response count, with both suppressed below three submitted responses. | **Owner: Danny Tran, program owner of record.** The implementation remains responsible for tenant scope, verified-principal self-scope, attendance-backed eligibility, and keeping student identity and comments out of Connector responses. | Student rating submission and Connector aggregate review. **Amendment and withdrawal only, for now:** no student-authorized route returns a speaker id, so a first rating cannot be reached through a UI until OQ-CBA-064 is answered. |
| OQ-CBA-004 | Does CBA scoring amend ADR-0011 for a customer-directed neutral Topic score and virtual-event weight redistribution, or use an explicitly labeled CBA scoring-mode policy? | **Closed 5 September 2026 by ADR-0016 (Accepted).** ADR-0011 is not amended; it is extended with a third evidence state. `policy_neutral` is a customer-directed policy value distinct from `unknown`, and only `unknown` makes a composite unscorable — weights are never re-spread per candidate. Missing topic evidence contributes `0.50` under the named, versioned constant `CBA_NEUTRAL_TOPIC_VALUE` (`cba-neutral-topic 1.0.0`), carried on every score that used it. `REGISTRY_VERSION` moves to `2.0.0-approved-oq-cba-004`; runs pinned to `1.1.1-approved-g1-m6j` stay readable but are never compared across the bump, and scoring mode is a separate pin from registry version. See ADR-0016 Proposals 1, 2, 7 and 9. | **Owner: Danny Tran, Development Lead/program owner of record, with the matching-domain reviewer. Decision is a hard pre-merge gate for the first Wave 3 registry PR.** | `REGISTRY_VERSION` 2.x, neutral Topic behavior, virtual redistribution, new golden cases. |
| OQ-CBA-005 | Should ranked candidates expose any overall score in the UI? | Rank internally; show factor-level provenance and Topic reasoning; do not show a prominent overall percentage. | CBA product owner; decide only if a later UI asks to expose the aggregate. | Optional match-result presentation change; not ranking itself. |
| OQ-CBA-006 | Which named capability, if any, owns the `jobs`, `redrive`, `engagement`, and `review` routers? `CBA-SCOPE-POLICY` classified them as capability-neutral infrastructure — the durable command/job substrate every capability rides on — and mounts them unconditionally. | Keep them unconditional. There is no version of this product that offers match runs or imports but not the job lifecycle that carries them, so gating them would break a preserved capability rather than gate an out-of-scope one. Do not invent a capability name for them merely to make the table look complete. | CBA product owner with the API owner; decide only if a later track needs one of these surfaces gated independently. | Any future per-surface gating of the command/job substrate. |
| OQ-CBA-007 | Does the worker (`services/worker/smartmatch_worker/`) need to read the same product-scope policy, and if so which worker behavior changes with it? | The worker reads no scope today and `CBA-SCOPE-POLICY` added none: no current worker command behaves differently per product, and a scope value the worker ignores is worse than no scope value at all. `smartmatch_domain.product_scope` is already importable from the worker if a later track needs it. The prohibition on new CBA `member_inquiry` writers and on live crawl/paid-extraction execution stays enforced where it already is, not by a new worker flag. | CBA product owner with the worker owner; decide before any Wave 5 track proposes a scope-dependent worker command. | Scope-aware worker command routing, if it is ever needed. |
| OQ-CBA-008 | Must a speaker's Industry/Role classification carry provenance and a correction history — who assigned it, whether it was inferred or human-assigned, and what the previous value was? Customer §19 infers a classification and then has a Speaker Connector correct it, and §§7–8 both require the correction to be possible, but no audit requirement for the correction itself has been stated. | `CBA-DATA-SCHEMA` (migration `0024`) stores the **current value only**, the same current-state-only treatment P9 Gate A §2 gives `board_role`. Do not invent an `industry_source` vocabulary or a history table to answer this; a guessed provenance vocabulary is a permanent default that nobody approved. If the answer is yes, the shape is `contact_channel_transition`'s and it is a later revision. | CBA product owner with the privacy owner; decide before `CBA-IMPORT-CLASSIFY` writes an inferred classification. | Classification audit trail, "who classified this speaker" in the Connector UI, and any inference-provenance display. |
| OQ-CBA-009 | Should `professional_unit_relationship.professional_id` (migration `0012`) and `contact_channel.professional_id` (migration `0021`) be retrofitted with the foreign key to `user_account` that `speaker_profile.professional_id` now carries? Both columns' own docstrings say "whichever migration gives professionals a persisted identity should add this foreign key alongside it", and Choice A of the synthetic pilot authorization has since made `user_account` that identity. | Leave both columns as they are. Migration `0024` added the key only on its own new table, where no rows exist to reconcile. Retrofitting a foreign key onto tables that already hold rows is a data-bearing change with its own backfill and orphan-handling decisions, and riding it along on an expand-only revision would hide those decisions inside another card. | Database owner with the pilot data owner; decide before any track depends on a contact having an account. | Referential integrity for the two older `professional_id` columns; a single professional-identity story across all three tables. |
| OQ-CBA-010 | Where does a **quarantined** CBA classification live — a raw industry or role value a Speaker Connector's import produced that neither taxonomy recognizes? Both taxonomy modules return a `Quarantined…` value carrying the reviewer's original text precisely so it is not dropped, and migration `0024`'s columns store released codes and refuse everything else. | Keep quarantine where it already is: import quarantine is `review_item`'s job (architecture v1.1 §1.5, "a validated import produces review items, not verified records") and discovery quarantine is `discovery_review_item`'s. Do not add a raw-value column beside the classification, which would create a second quarantine with no review queue attached to it. | CBA product owner with the import owner; decide as part of `CBA-IMPORT-CLASSIFY`. | Connector review of unrecognized industry/role values, and the input to the next taxonomy version. |
| OQ-CBA-011 | May SmartMatch **store** a speaker's personal "Contact Email" (customer §18) supplied on a CPP CBA contact import, and under what retention, access, and deletion rules? P9 Gate B does not answer it: that gate decided the three *published* contact fields on the `events` dataset — details an organizer has already published on a public event page — and §§B2/B3 of its worksheet turn on exactly that published-ness. A personal address on a coordinator's spreadsheet is different data, and Gate B still required a named privacy owner and the full ADR-0014 field set to answer the easier question. | `CBA-IMPORT-CONTRACT` declares `professionals.contact_email` under `gate_pending` with posture **`withhold`**: the column is recognized (an export carrying it is never refused and never reported as `unexpected_columns`), the value is dropped before anything is written, and the drop is reported as a `columns_withheld_pending_gate` **warning** naming the column. Do not guess a collection policy because §18 mentions the field. Note this is **not** a consent question — no answer to it can make an imported address send-eligible, because `smartmatch_domain.consent` admits four approved sources and an import is none of them. | CBA program owner **and** a named privacy owner, recording ADR-0014's fields as P9 Gate B did; decide before any card persists a speaker contact address. | Storing and displaying a speaker's contact email; Connector-initiated invitations that need an address to reach. |
| OQ-CBA-012 | Two questions about a CBA-era `professionals` export that `CBA-IMPORT-CONTRACT` deliberately left open. **(a)** Should `metro_region` remain **required**? It was ratified for the IA West pilot, and customer §10 now measures proximity in miles from CPP using city and/or ZIP, both of which are declared optional. An export carrying a ZIP but no metro region currently fails `missing_required_columns`. **(b)** Should the contract adopt the customer's own header text for the three §18 fields already declared under an earlier spelling — "Company Name" for `company`, "Current Position" for `title`, "Topic/interests/expertise text" for `expertise_tags`? | Leave both as they are. `normalize_header` is case-, whitespace- and punctuation-insensitive, not a synonym table, so declaring both spellings would put the same customer field in the contract twice — the one failure `columns.yaml` exists to prevent. An export using the customer's wording today reports `unexpected_columns` (a **warning**), and `_normalize_row` does not drop the values, so the data still reaches review. De-ratifying a required column and adopting alias headers are both re-ratifications, not refactors. | CBA product owner with the pilot data owner, once a real CBA export has been seen; decide before `CBA-IMPORT-CLASSIFY` depends on either spelling. | A CBA export that validates with no warnings; dropping `metro_region` in favour of city/ZIP proximity. |

| OQ-CBA-013 | Must a physical Speaker Request name a location? Customer §12 says an Event Host can "specify event location" — a capability, not an obligation — while §10 makes Proximity 30% of the default score and says city or ZIP is sufficient for this phase. `CBA-EVENT-REQUEST` had to answer this to ship and answered fail-closed rather than guessing a permanent default. | A physical request must name a city or a postal code; `smartmatch_domain.speaker_requests` refuses one that names neither, so no request is stored that the largest single factor cannot score. A virtual request must name none — `ck_event_virtual_has_no_location` and §11's "ignore Proximity entirely". Do not relax either arm by adding a nullable default; the interim behaviour is a refusal a host can read, not a silent value. | CBA product owner with the matching lead; decide before `CBA-PROXIMITY` fixes the band scores. | Accepting a placeless physical request, if the customer says Proximity may legitimately be unknown for one. That is an ADR-0011 question about unknown-not-zero, not a validation preference, and it changes what `CBA-PROXIMITY` must score. |
| OQ-CBA-015 | §13's "add a contact" form collects a personal contact email, and OQ-CBA-011's ratified posture withholds it. Should the create surface keep collecting a field the system always discards, or should the field leave the form until the privacy gate answers OQ-CBA-011? | `CBA-CONTACT-MANAGEMENT` mirrors the import path's withhold posture exactly: `contact_email` is **recognized** in the create and update bodies (a client sending it gets a `201`, not a validation error about a field the form legitimately has), the value is **never persisted** — not in `speaker_profile`, not in `user_account.email`, which is a derived RFC 2606 `.invalid` placeholder, and above all not in `contact_channel`, which this card writes on no path — and the discard is **reported** in the response's `withheld_fields`. Reporting is the load-bearing part: a Connector who types an address and sees no complaint will conclude the contact can be emailed, which is the belief the withhold posture exists to prevent. Do not remove the field silently either; a form that drops an input a Connector expects is its own confusion. This is **not** a consent question — no answer to it makes the address send-eligible, because `smartmatch_domain.consent` admits four approved sources and typing an address into a form is none of them. | CBA program owner **and** the named privacy owner answering OQ-CBA-011, recording ADR-0014's fields; decide together with OQ-CBA-011 rather than separately, since one answer settles both surfaces. | Storing a manually entered speaker contact address; a Connector-initiated invitation that needs a real address to reach; removing the field from §13's form. |
| OQ-CBA-016 | What `board_role` should a manually created speaker contact's `professional_unit_relationship` row carry? Migration `0012` deliberately declined to invent a vocabulary for that column and left it `NOT NULL` free text, so a §13 create is forced to write *something* about a person the customer requirements describe only as a professional contact. | `CBA-CONTACT-MANAGEMENT` writes the literal `cba_speaker_contact` (`smartmatch_domain.cba_contacts.CONTACT_BOARD_ROLE`), chosen to describe **how the row got here** rather than what the person does — describing the person is what a vocabulary would do, and no committed artifact supplies one. Do not read it as a classification, do not branch on it, and do not grow it into a set of role values by adding siblings one card at a time; that is how a free-text column acquires an unratified vocabulary nobody agreed to. The speaker's actual role category is `speaker_profile.primary_role_code`, which is held to customer §8's closed list. | CBA product owner with the pilot data owner; decide before any surface renders `board_role` to a user or any query filters on it. | A ratified `board_role` vocabulary; telling a board member, a speaker contact, and an imported professional apart among one unit's relationship rows. |
| OQ-CBA-017 | Two genuinely different people share a name inside one org unit. `CBA-CONTACT-MANAGEMENT` derives a contact's identity as `uuid5(namespace, "tenant:unit:folded_name")`, so the second person derives the first person's `speaker_profile` primary key. What should the second create do, and does the answer require a stored disambiguator the schema does not have? | The create is refused with `409 speaker_contact_name_already_used`, naming the stored contact so a Connector can recognize or dispute it. Both silent alternatives are worse: upserting overwrites the first person's company, title and classification while looking like a successful save, and inserting a second row is not available because the derived id *is* the primary key. Do not add a uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` — the database would then refuse the write outright rather than answerably — and do not append a discriminator to the name to force the hash apart, which stores a lie about what the person is called. Note the related asymmetry `smartmatch_persistence.cba_contacts.update` documents: an *edit* that changes a name does not move the derived key, so a later create under the corrected name succeeds and produces a second contact for one person. | CBA product owner with the pilot data owner; decide before a unit's roster is large enough for a collision to be likely, and before any card offers a merge. | Recording two same-named professionals in one unit; a merge or disambiguation surface; whether identity should stop being name-derived and become an opaque id with the name as an ordinary attribute. |
| OQ-CBA-014 | May an Event Host list back the Speaker Requests they filed? Customer §12 gives a host the create; §13 gives "view incoming Speaker Requests" to the Speaker Connector and names nobody else. Under deny-by-default the absence of a permit is a denial, but the silence is about the *host's own* requests rather than about the queue. | `GET /v1/units/{unit_id}/speaker-requests` is `admin`/`coordinator` only, so the queue — which carries every host's request text for the unit — reaches only the Speaker Connector. A host is not left blind: `POST` returns the stored row on a first filing and returns it again on every resubmission, because ADR-0012's identity key makes a re-file the same request. Do not widen the list role set as a shortcut; a host-scoped read is a different query, not a wider permit. | CBA product owner with the API owner; decide before any Event Host surface asks for a list. | A host-scoped Speaker Request list (filtered to the requests that host filed), the role set that would carry it, and whether "filed by" needs a stored actor column the `event` table does not have today. |
| OQ-CBA-018 | Where does a student's **event registration** live? Customer §15 asks that students "register for events" and this schema has no table for it. `attendance_record` is the only link between a student and an event, and it is attendance: ADR-0013 makes it the sole input to points, `ck_point_ledger_entry_kind` derives every `attendance_credit` from a `source_attendance_id`, and `uq_attendance_record_subject_event` means a row written at registration time is indistinguishable from one written at check-in. | **Answered by `CBA-STUDENT-REGISTRATION`, migration `0026_event_registration`.** A registration lives in its own `event_registration` table, with exactly the columns this row specified while it was open — `id`, `tenant_id`, `owning_unit_id`, `event_id`, `subject_id`, `status`, `registered_at`, `updated_at`, unique on `(tenant_id, subject_id, event_id)` so a resubmission is idempotent without an `Idempotency-Key`. `ck_attendance_record_method` is **unchanged** and gained no fourth value; `event_registration` holds no foreign key to `point_ledger_entry` in either direction and no `(tenant_id, id)` uniqueness for one to be built on, which `tests/integration/test_event_registration.py::TestRegistrationHasNoPathToPoints` asserts against `pg_constraint`. Cancellation is a status transition and not a `DELETE`, so "cancelled" and "never registered" stay different facts. That test file — which previously *pinned the absence* — is now the registration suite, and its `TestAttendanceStaysAttendance` controls are kept unchanged, because the hazard they guard grew rather than shrank once a Register control became real. Two questions this answer deliberately did not settle are recorded below as **OQ-CBA-021** (capacity and waitlist) and **OQ-CBA-022** (a transition history). | Answered. Originally CBA product owner with the database owner; the two residual questions carry their own owners below. | Delivered: student registration and cancellation; an agenda that means "what I am going to" as well as "what I was recorded at"; and an .ics download for an event a student has registered for rather than only one they attended. Capacity and waitlist behaviour remains gated on OQ-CBA-021. |
| OQ-CBA-019 | Should `admin` and `coordinator` be able to read the **student** event surfaces (`GET /v1/units/{unit_id}/student/events` and `/student/agenda`)? They are the only operations in `tests/authz/test_policy_matrix.py` that an `admin` at the tenant root cannot perform, which is unusual enough to be worth deciding rather than inheriting. | `{student}` and nothing else. Customer §15 names the Student and nobody else, and under deny-by-default the absence of a permit is a denial. A Connector is not shut out of the data: `events.list` serves the same rows with the review status and extraction provenance the student surface drops, and `engagement.attendance_summary.read` serves attendance as an aggregate carrying no subject ids. Do not widen the role set as a shortcut to "let a Connector see what a student sees" — one student's *agenda* is that student's own list, and a Connector-facing view of who is attending what is a different query with a different privacy question, not a wider permit on this one. | CBA product owner with the API owner; decide before any Connector surface asks to preview the student portal. | A Connector-visible student event view; any "see this as a student" affordance; and whether per-student attendance is readable by a Connector at all, which today it is not. |
| OQ-CBA-020 | Should the month calendar at the bottom of the student Events page become interactive — a day that opens the events on it, or filters the lists above? Customer §15 requires the grid be kept there and says nothing about behaviour, while `docs/architecture/engagement-model.md` §5 (D-11) argues a month grid is the wrong primary surface for a student. | The grid renders the same events as the two lists above it and its cells are **not** interactive. The action a cell would most naturally offer is registration, which does not exist (OQ-CBA-018), and a day that opened a dialog with a disabled button would be a worse answer than a day that opens nothing. It is not a mock either: B09's `MockStudentCalendar` is gone and every cell is drawn from a server response, so an empty grid means an empty month rather than an unwired one. Do not add cell click handlers that only scroll — a control that looks like a filter and is not is the defect class this page was rewritten to remove. | CBA product owner; decide once registration exists, since the interaction worth having depends on what a day can offer. | Day-level drill-down, filtering the lists from the grid, and week or day views on the student page. |
| OQ-CBA-021 | Once `speaker_profile` identity is opaque (OQ-CBA-017) and the same-name `409` is gone, what warns a Speaker Connector that the contact they are creating may already exist? | **Open.** Nothing warns them today beyond the `409` that OQ-CBA-017 removes. Do not reinstate a uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` to fill the gap — that refuses the write outright rather than answerably, and re-creates the problem opaque identity was chosen to solve. A likely-duplicate *hint* on create, and a merge surface, are the shapes worth considering. | CBA product owner with the pilot data owner; decide before a unit's roster is large enough for accidental duplicates to be routine. | Duplicate speaker contacts in one unit; any merge or de-duplication surface; whether a create ever blocks rather than warns. |
| OQ-CBA-023 | What is the **CPP campus coordinate of record** that every proximity distance is measured from? ADR-0016 Proposal 3 approved the band table and the unit (miles from the CPP campus) but named no point. A campus is roughly two miles across, so the choice of point can move a speaker across the 25-mile edge. | **Closed 5 September 2026 by Danny Tran, program owner of record.** The owner **ratified the approximate centroid** of 3801 W. Temple Ave, Pomona, CA 91768, on the stated reasoning that a sub-mile origin error almost never flips a candidate across the 25- or 75-mile edge. `CPP_CAMPUS_ORIGIN_VERSION` moves `0.1.0-provisional` → **`1.0.0`**, and the "PROVISIONAL: not confirmed by the program owner" sentence leaves the `source` string, replaced by "Ratified -- not surveyed -- by the program owner on 2026-09-05 (OQ-CBA-023)". **Ratified is not surveyed**, and the source string says so deliberately: a later card proposing a surveyed coordinate is refining a value an owner accepted, not correcting one nobody approved. No geocoding provider was called to obtain it and none is called to use it. The coordinate, the band table and `CBA_PROXIMITY_FORMULA_VERSION` are all unchanged by the promotion — that the origin could move version without touching the formula is the config seam working as designed. | Closed by the program owner; implemented by `CBA-MATCH-REGISTRY`. | **Unlocked:** proximity golden cases G-CBA-04 through G-CBA-07, now approved and reproducible against a `1.0.0` origin. |
| OQ-CBA-024 | How does a speaker's `location_city` / `location_postal_code` become a coordinate? §10 says "City or ZIP code is sufficient for this phase" but names no resolution method, and live geocoding is gated by `ALLOW_LIVE_PROVIDERS=false`. | **Decision recorded 5 September 2026; the ZIP half implemented 6 September 2026.** The owner's steer was a **static offline ZIP-centroid table** — deterministic, no network, classroom-safe, with a ZIP absent from the table resolving to `unknown` and never to a guess — and that is what shipped. `python/smartmatch_domain/smartmatch_domain/zcta_centroids.py` is generated at build time by `tools/generate_zcta_centroids.py` from the US Census 2023 national ZCTA Gazetteer and the 2020 ZCTA-to-County relationship file, both public domain and both pinned by SHA-256; every coordinate is copied from the Gazetteer unmodified and none was typed, rounded, interpolated, or estimated by hand. California only (1,808 ZCTAs — any part in a state-FIPS `06` county), because a national table is a wider claim than anybody made. `smartmatch_api.zip_proximity.resolve_distance_from_campus` reads it at the API/evidence layer, and `factors/proximity.py` still refuses to resolve anything itself, still takes an already-resolved `distance_miles`, and now records the distance's provenance (`zcta-centroid-2023`) in the score's `basis` under an estimate label that names the ZIP-centroid approximation (ADR-0011). A blank, malformed, or non-Californian ZIP resolves to `None`, which is **unknown** — never a nearest neighbour, a state centroid, or the Far band — and such a candidate is excluded from the match-run pool and reported, not entered at `0.0` and ranked last. **The city half is not done and was not attempted:** `speaker_profile` has no state column, so a city name alone is ambiguous, and that is **OQ-CBA-063**. | CBA product owner with the data owner; the ZIP resolution is built and needs no further decision. Re-open only to widen the table beyond California, to move the Gazetteer vintage, or to answer OQ-CBA-063. **No longer blocks OQ-CBA-031.** | Physical-event proximity that produces measured values rather than unknowns — done: `POST /v1/units/{unit_id}/match-runs` no longer refuses a physical Speaker Request with `match_run_physical_scoring_unavailable`. |
| OQ-CBA-025 | When does `factors/travel_burden.py` — the pre-CBA continuous kilometer penalty approved at gate G1 — retire, and what happens to runs stored under `1.1.1-approved-g1-m6j`? | Both factors coexist. `travel_burden` is neither deleted, rewritten, nor re-scaled by the CBA proximity work; the two carry independent formula versions and `proximity` deliberately duplicates the haversine rather than importing it, so a change to one cannot move the other. Stored 1.x runs stay readable at their own pin and are never re-scored (ADR-0016 Proposal 9). Retirement is a question about stored runs and the registry's declared factor set, not a cleanup. **Implemented as *coexist* by `CBA-MATCH-REGISTRY` (5 September 2026), and still open as a retirement question.** The 2.x registry gives `travel_burden` — and, on the same reasoning, `topic_relevance` — `retired_in_version="2.0.0-approved-oq-cba-004"`: both stay declared, stay `implemented=True`, and keep the weights they were approved with, but `FactorSpec.active_weight` returns `0.0` so they leave the current model's numerator and denominator **together**. Removing one without the other is precisely the legacy deflation defect, pointed the other way, and `tests/unit/test_factor_registry.py` asserts it cannot happen. `SUPERSEDED_G1_MODEL` names the old pair so a 1.x run is reproduced by name rather than by remembering which two factors used to be in force, and `score_candidate` pins `1.1.1-approved-g1-m6j` explicitly rather than reading `REGISTRY_VERSION`. **Deletion still waits on the original condition:** no pinned run may reference the factor. **The blocker on that condition has changed.** OQ-CBA-031 landed on 6 September 2026, so no surface produces a *new* 1.x run any more: `POST /v1/units/{unit_id}/match-runs` scores through `rank_cba_candidates`, and neither `rank_candidates` nor `score_candidate` is called from `services/` at all — `SUPERSEDED_G1_MODEL` survives there only on the worker's read path (`resolve_scoring_model`), which reproduces a stored run rather than producing one. What the condition now turns on is **stored** runs: whether any `match_run` row still pins `1.1.1-approved-g1-m6j`, which is a question about data in a database and not about this tree. Retirement itself is unchanged and undecided — this note records that the stated blocker has moved, not that the answer has. | Matching lead with the CBA product owner. OQ-CBA-031 has landed and no surface produces a 1.x run; what remains is a check of stored runs and then the retirement decision itself. | Removing the pre-CBA factors, and a single proximity notion in the registry. |
| OQ-CBA-026 | Which semantic model performs customer §9's Topic comparison, on whose credentials, under which vendor terms, and at what per-run cost? The input is a named person's professional profile text, so this is also a privacy question: may a speaker's `topic_text` and `prior_talk` be sent to a third party at all, and under what retention? | `CBA-MATCH-TOPIC` ships **no live adapter**. `smartmatch_providers.topic_semantics.build_semantic_topic_provider` returns the deterministic playback fixture by default and refuses a live client under **every** edition — not only the classroom one — on the pattern `build_paid_extraction_provider` established, because the refusal is a property of what has been approved rather than of where the code runs. A model credential found in any environment fails closed. `ALLOW_LIVE_PROVIDERS=false` is a necessary gate and explicitly not a sufficient one: flipping it reaches an adapter that does not exist. Do **not** answer this by adding a token-overlap scorer and calling it semantic — a lexical comparison shipped under a semantic name puts an untrue claim about how every stored match was produced into the data permanently, which is why the fixture reports `is_semantic_model = False`. | CBA product owner with a named privacy owner (the same pairing OQ-CBA-011 and OQ-CBA-015 require), recording ADR-0014's field set; decide before any card enables a live Topic comparison. | A real §9 semantic Topic comparison; per-run cost ceilings for it; and sending speaker profile text to any external provider. |
| OQ-CBA-027 | Which factor key does the CBA registry bind to its Topic slot — the pre-CBA lexical `topic_relevance`, or the new three-state `cba_semantic_topic`? Both now exist and they measure different things: the first is set overlap between recorded expertise tags and an `event_need`'s declared topics, the second is §9's description-to-profile comparison carrying ADR-0016's `policy_neutral` state. | **Closed 5 September 2026 by `CBA-MATCH-REGISTRY`, the Wave 3 registry track this question was assigned to.** The Topic slot binds **`cba_semantic_topic`**, at weight 0.15. The reason is that §9 states two requirements and `topic_relevance` implements neither: §9 asks for a comparison against *the request's description* (lexical tag overlap does not read a description at all) and states a **neutral policy for an observed absence** (a two-state factor has no way to carry one, and would have to return either a fabricated 0.5 or an unknown — the first is ADR-0011's defect and the second discards the customer's stated policy). Binding the slot to `topic_relevance` would have satisfied the weight table while failing the requirement the weight exists for. **What happens to `topic_relevance`:** it is *retired, not deleted*, exactly as `travel_burden` is under OQ-CBA-025 — still declared, still implemented, still carrying the 0.70 it was approved with, and marked `retired_in_version="2.0.0-approved-oq-cba-004"` so it contributes to no current model's numerator *or* denominator. That is what keeps runs pinned to `1.1.1-approved-g1-m6j` reproducible, and `tests/unit/test_matching_approved_golden.py` asserts three of them still reproduce after the bump. Both factors were kept out of one Topic slot, as required. | Closed by `CBA-MATCH-REGISTRY`. | **Unlocked:** CBA Topic scoring in the registry, and golden cases G-CBA-01, G-CBA-02, G-CBA-03 and G-CBA-11, all now landed. |
| OQ-CBA-028 | Should `match_run` carry **`scoring_mode` and `scoring_mode_version` as columns**? ADR-0016 Proposal 9 requires a run to record its mode, and `MatchRunPins` now does — but `CBA-MATCH-REGISTRY` added no DDL, so the mode is not on the row. | **Closed 6 September 2026 by Danny Tran, program owner of record.** The owner **ratified two NULLABLE columns and declined the backfill**, implemented by migration `0032_match_run_scoring_mode`. `scoring_mode` and `scoring_mode_version` are text, nullable, and constrained by a **partial** CHECK — `ck_match_run_scoring_mode`, reading `scoring_mode IS NULL OR scoring_mode IN ('cba-physical-1','cba-virtual-1')` — which holds ADR-0016 Proposal 5's closed vocabulary only where a value is present. `ck_match_run_pins_present` is **untouched**: widening it to require a mode would have put every row already stored in violation, which is the trap this entry warned about. **Existing rows were NOT backfilled, and that is a second decision rather than an omission.** A backfill from the stored explanation payload was written and reviewed; the owner rejected it. Reaching a stored run means an UPDATE, `0018`'s `match_run_is_immutable` refuses every UPDATE, and a migration that switches that trigger off — by name, by `session_replication_role`, or by drop-and-recreate — sets a **permanent precedent** the next revision wanting to "just fix these rows" would cite correctly. The gain did not justify it, because nothing is lost: the mode of a pre-`0032` run stays exactly as recoverable as it was, and the stored explanation payload remains its system of record. A pre-ADR-0016 run therefore **stays NULL** — the true statement that it has no mode rather than a gap, and a backfilled value would have been a reconstruction indistinguishable in the column from a mode the run itself recorded. `0032` writes no rows at all: two nullable columns, one partial CHECK, one partial index, no DML. The rejected *column* alternative was a NOT NULL column carrying a `legacy-pre-adr-0016` sentinel — minting a third value would reopen a closed vocabulary in DDL, and every reader of `scoring_mode` would have to know a mode that names no model. `registry_hash` would not have been an acceptable backfill source in any case: recovering a mode from a digest means recomputing today's weight sets, which is wrong for any unit with stored overrides (migration `0027`). The mode is **not** exposed on the HTTP read response; that is separate work with its own contract regeneration. | Closed by the program owner; implemented by `CBA-MATCH-RUN-SCORING-MODE` (migration `0032`). | **Unlocked:** mode-based filters, aggregates and indexes over `match_run` for every run recorded from `0032` onward — "how many virtual runs did we score in March" is one query over those. **Still not unlocked:** every run recorded *before* `0032` reads NULL and is not reachable by such a query, so the stored explanation payload remains the mode's system of record for the historical rows; labelling them, if a report ever needs it, is a read-side view or a report-time join rather than a rewrite of immutable rows. No route exposes the column. |
| OQ-CBA-029 | Does an event have a **capacity**, and what happens to a student who registers past it? Migration `0026`'s `ck_event_registration_status` admits `registered` and `cancelled` and deliberately not `waitlisted`, because nothing anywhere in this schema states how many students an event can hold: `event` carries no seat count and no `max_attendees`. | `event_registration.status` stays two values. A waitlist is overflow from a capacity, so admitting `waitlisted` now would put a state in the constraint that no writer could legitimately produce — a vocabulary invented by DDL ahead of the decision that gives it meaning, which is exactly what migration `0012` refused to do for `board_role` and what `0024` cites that refusal for. Do **not** add a nullable `capacity` column "for later" either: a capacity that is `NULL` for every event is a field the register route would have to decide the meaning of, and it would be deciding it silently. When a capacity is ratified, widening the CHECK is a two-line revision and the diff will say which card decided it. `tests/integration/test_event_registration.py` asserts the absence in both directions — the constraint definition carries no waitlist value, and an attempted `waitlisted` insert is refused by the database. | CBA product owner with the events owner; decide before any surface shows a student how many places are left, and before any card promises a student a place it cannot guarantee. | Capacity limits, a waitlist and its ordering (which `registered_at` already supports), "N places left" on the browse surface, and any rule about registering for an event that has already happened. |
| OQ-CBA-030 | Does a registration need a **history** of its transitions, or is the current status plus one `updated_at` enough? Migration `0026` stores the current value only, so a student who registered, cancelled, and registered again leaves one row saying `registered` and an `updated_at` that describes only the last move. | Current value only, the same treatment `0024` gives a classification under OQ-CBA-008. `registered_at` never moves and `updated_at` says when the status last did, which answers "how late was this cancellation" — the question that motivated keeping the row at all. It does **not** answer "how many times did they change their mind", and no column here pretends to: a `cancelled_at` recording one cancellation would quietly become wrong the moment there were two, which is why `0026` declines to add one. If the answer is yes, the shape is `contact_channel_transition`'s (migration `0023`) and it is a later revision — do not approximate it by accumulating rows in `event_registration`, which would break the uniqueness that makes registering idempotent. | CBA product owner with the events owner; decide before any coordinator surface reports churn, and before anyone asks how many students dropped out of an event. | A registration audit trail; churn and drop-out reporting; distinguishing "registered late" from "re-registered after cancelling", which today read identically. |
| OQ-CBA-031 | When does the **HTTP match-run surface** move from the superseded two-factor composition to the CBA four? **It has: the surface migrated on 6 September 2026** (`6df7006`, `297b856`). `POST /v1/units/{unit_id}/match-runs` calls `smartmatch_domain.scoring.rank_cba_candidates`, not `rank_candidates`, so it no longer produces a `1.1.1-approved-g1-m6j` run. The question is kept here, not deleted, because closing a register row is the owner's act and not a documentation edit. | **Shipped, and shipped as a request-schema change rather than a swap.** The route no longer records `scoring_mode: null`; it resolves the mode from the filed Speaker Request's `is_virtual` switch — `SpeakerRequestEvidence.scoring_mode` is a property with no constructor parameter, so there is no body field through which a caller could choose one — and the worker pins the rulebook that actually produced the utilities. The evidence `score_cba_candidate` needs (a resolved primary sector, a resolved role category, `SpeakerTopicEvidence` distinguishing "profile read and empty" from "no profile row", a city/ZIP, and a resolved `distance_miles`) is assembled **server-side** by `assemble_cba_pool` from this tenant's own rows, which is why the body carries `candidate_subject_ids` and not evidence. Weights come from `factor_registry` plus this unit's persisted overrides, read from the authorized unit and never from the body, so both ends fingerprint one set. It no longer depends on OQ-CBA-024: the static ZIP-centroid table shipped 6 September 2026, so a physical candidate with a Californian ZIP resolves to a measured `distance_miles` and a shortlist can be filled. A candidate whose location is still unresolvable stays `unknown` and is excluded from the pool and reported, never entered at `0.0`. Two module docstrings — `routers/match_runs.py` and `match_run_evidence.py` — still describe the pre-migration `rank_candidates` behaviour and are stale against their own code. | CBA product owner with the API owner and the matching lead; decide before any CBA match run is expected to be reachable by a client. | CBA four-factor scoring over HTTP; virtual-event match runs from a real Speaker Request; and the first stored run pinned to `2.0.0-approved-oq-cba-004`. |
| OQ-CBA-032 | Must a matching-weight change pass a **shadow evaluation** before it takes effect? `docs/plans/workshops/g1-workshop-output-worksheet.md` agenda item 4.2 — ratified 2026-09-03 — answers "**Yes** — no weight change ships without shadow evaluation pass", and customer §5 says a Speaker Connector may adjust them. `CBA-MATCH-WEIGHTS` implements the second and does not build the first. | **Answered 6 September 2026 by Danny Tran (@BrooklynD23): no shadow-evaluation gate for the pilot.** A change takes effect for the **next** run and never for a run already recorded. The compensating control is the reason, and it is real rather than a promise: `match_run` stores the weights it scored with and migration `0018`'s `match_run_reject_mutation()` trigger raises `restrict_violation` on any `UPDATE`, so no historical result moves when a weight does; every accepted change is versioned and attributed in `match_weight_setting_revision` (migration `0027`, `changed_by_user_id` `NOT NULL`, unique on `(tenant, unit, version)`, itself held immutable by `match_weight_setting_revision_is_immutable`), written by `python/smartmatch_persistence/smartmatch_persistence/match_weight_settings.py`; and `expected_version` with its `409 matching_weights_stale` in `services/api/smartmatch_api/routers/matching_weights.py` stops one Connector's save from silently discarding another's. So a weighting that turns out to be wrong is attributable and reversible by making another change. **Why no gate was built, precisely.** An advisory shadow run needs a candidate pool, and there is none to take: `MatchRunRequest` carries a `speaker_request_id` and an explicit `candidate_subject_ids` list, so a pool exists only where a live request submitted one, and a unit adjusting its weights outside a request has nothing for a shadow run to score. Over synthetic pilot data a shadow evaluation tells you close to nothing while *looking* like validation, and shipping it would invite the reading that these weights had been validated when they had not. A gate nobody specified would be a policy invented in code. **Two corrections to this row's earlier framing.** MM-005's shadow mode is a *different* control and is already satisfied: it governs machine-proposed adjustments, and `smartmatch_domain.feedback.WeightProposal.requires_approval` is a setterless property on a `@final`, frozen, slotted class, so a proposal can never apply itself — nothing in this repository consumes those proposals at all. The gate this row is about is the ratified G1 worksheet's, not MM-005's. And the v1.1 Appendix B text both claims rest on is not vendored here (`migration-manifest.yaml` records `contract_refs_status: UNVERIFIABLE` for MM-005), so nobody in this repository has read it. Note also that `expected_version` is optional: a client that omits it gets last-write-wins, and the revision log records that too. **Re-entry trigger — this is the thing being written down, not the gate.** Revisit before the first time a unit's weights are adjusted against a **live cohort** rather than a synthetic one. Costed at the time of the decision: an advisory shadow run ~2–3 days and no DDL; a blocking gate with a stored evaluation record ~1 week and a migration. | Answered 6 September 2026; recorded in full under **Decision taken 2026-09-06 — OQ-CBA-032** below. Danny Tran (@BrooklynD23), the program owner named for ongoing weight governance by the same ratified worksheet (agenda item 4.1). Reopens on the trigger above — a live cohort — and is decided then with the matching lead. | Delivered: governed weight changes for the pilot — versioned, attributed, refused when stale, and unable to move a run already recorded. Still gated on the trigger: any before/after comparison of two weightings over the same pool, and any claim that a weighting was evaluated before it shipped. |
| OQ-CBA-033 | Should a weight override be scoped **per scoring mode**? A unit runs both physical and virtual events, and today one override map serves both — the virtual model simply never reads the `proximity` entry (customer §11). | One map per unit, not one per mode. `normalize_weights` is already given the model, so a mode that does not score a factor ignores its override without a second row needing to say so, and `validate_weight_overrides` refuses any proposal that would leave *either* current model summing to zero — so a setting cannot be admissible for one event shape and broken for the other. A per-mode setting would be a second row shape and a second thing to keep consistent, for a distinction no requirement has drawn. | CBA product owner; decide if a Connector asks to weight a virtual event differently from a physical one. | Per-mode weighting, and a settings surface that presents the two event shapes separately. |
| OQ-CBA-034 | How long is the matching-weight **revision log** kept? `match_weight_setting_revision` is insert-only and nothing prunes it. | Nothing prunes it, and nothing should until a retention period is stated. The rows are small, one per accepted change, and a change is a rare policy decision rather than a workflow step — the same current-state-plus-log treatment `contact_channel_transition` gets, with the same unanswered retention question. The table is deletable per tenant (it is in `tests/integration/conftest.py`'s teardown tuple) so a tenant stays removable; only the `UPDATE` is refused. | CBA product owner with the privacy owner; decide alongside the retention questions already open for the other transition logs. | A retention policy for the audit trails, and any pruning job that would implement it. |
| OQ-CBA-035 | Where does a **self-service opt-in** actually happen? `smartmatch_domain.consent` has admitted `self_service` as an approved source since v1.1 §2.3, and `CBA-CONTACT-LIFECYCLE` now lets a Speaker Connector record a channel *citing* it — but no surface in this repository lets a **speaker** opt in for themselves. Every approved source reaching storage today is therefore a Connector's assertion about a conversation that happened elsewhere. The same gap applies to a **production roster import**: no import path produces a consented channel, and OQ-CBA-011 is why. | The four approved sources stay exactly as they are, and the absence of a self-service surface stays visible rather than being papered over. What a Connector records is an assertion **plus its evidence** — `consent_evidence` is required with every source and refused as a `400` when absent — so "who says this person agreed, and what can they show me" is answerable for every row without a self-service form existing. Do **not** build an "invite to opt in" flow to close this gap: an email asking an address to consent is itself outreach to someone who has not consented, which is the prohibited shape v1.1 §1.8 names and the reason no research state reaches `active_candidate`. Do not add an import path that writes `consented` either; an import is not one of the four sources, and OQ-CBA-011 already refuses to store the address such a path would need. | CBA product owner with the named privacy owner answering OQ-CBA-011; decide before any speaker is expected to opt in without a coordinator present, and before any roster import is asked to produce sendable contacts. | A speaker-facing opt-in surface producing a genuine `self_service` or `authenticated` record; a production roster import that yields contactable people rather than records; and the first consent in this system whose evidence is a form submission rather than a coordinator's note. |
| OQ-CBA-036 | **Two surfaces now write `contact_channel`**, and only one of them checks a roster. `routers/cba_contact_channels.py` requires the professional to be on the calling unit's §13 roster and answers `404` otherwise; `routers/outreach_contacts.py` stores whatever `professional_id` the caller names, because when it shipped no professional table existed. A principal holding both role sets — and `_OUTREACH_ROLES` and `_SPEAKER_CONTACT_ROLES` are the same two roles today — can therefore reach the weaker create. Should the generic route now gain the check, or should the two converge? | Both routes stay, and the generic one keeps taking the caller's word. It is not a bypass of an authorization control: both surfaces gate on `admin`/`coordinator` against the same `org_unit`, both refuse an unapproved source, both refuse a suppressed address, and `ck_contact_channel_sendable_consent` refuses a send-eligible row with research provenance no matter which route wrote it. What differs is a **data-quality** precondition, not a permission — and retrofitting it onto `outreach_contacts` would break every caller storing a professional this schema cannot resolve, which is the situation migration `0012` documents and OQ-CBA-009 is still open about. Do not delete either route to force the question; do not widen `_SPEAKER_CONTACT_ROLES` to `_OUTREACH_ROLES` or vice versa, which would make one widening argument serve two surfaces. | CBA product owner with the API owner, after OQ-CBA-009 settles whether `contact_channel.professional_id` gets its foreign key; decide before a CBA deployment enables `CONSENTED_OUTREACH` for callers who are not Speaker Connectors. | Collapsing the two contact-channel surfaces into one; a roster precondition on the generic route; and a single answer to "which people may this unit hold addresses for". |
| OQ-CBA-037 | May a channel's **address** be corrected? Neither surface allows it, and the refusal is deliberate on `routers/outreach_contacts.py`'s stated reasoning: changing an address moves a recorded consent onto a different person, which is the one edit that can never be a correction. But a Connector who mistypes an address is left with a dead channel carrying a real trail, plus a second channel for the same person, and nothing marks the first as the typo it was. | Leave the address immutable. The available remedy is to move the mistyped channel to `rejected` — a terminal state, recorded with an actor and a reason in the person's own words — and create the correct one, which leaves both the mistake and its acknowledgement on the record. Do not add a `PATCH` for the address: the consent evidence stored beside it names a conversation with a specific person, and there is no way for the system to know whether a new address reaches that same person. Do not delete the mistyped row either; deleting the evidence of a mistake is how an audit trail stops being one. | CBA product owner with the privacy owner; decide if typo'd channels become common enough that `rejected` rows are noise rather than history. | An address correction that carries its own justification; distinguishing "this person declined" from "this address was wrong" among `rejected` channels, which today read identically. |
| OQ-CBA-038 | May a Speaker Connector **suppress** an address from the §13 roster path? `CBA-CONTACT-LIFECYCLE` offers no suppression write: the only writers are `routers/outreach_contacts.py`'s `PATCH` and the public unsubscribe route. A Connector told in person "please stop writing to me" must therefore either reach the outreach surface or leave the instruction unrecorded, and an unrecorded instruction is the one failure mode the whole suppression design exists to prevent. | Not added here, because suppression is one authoritative list and a second writer is a second idempotency rule for the same instruction. The existing `PATCH` is reachable by the same two roles against the same unit, so a Connector is not blocked — they are one surface away, which is a usability gap rather than a control gap. What this card **does** enforce is that suppression, wherever it was written, wins on this surface too: a suppressed address cannot be created and cannot escalate, and `tests/integration/test_contact_lifecycle.py` asserts no `active_candidate` row exists joined to a suppression record. Note OQ-009 (r4) is still open on the other half — un-suppressing is refused rather than honoured, because reinstating an address nobody has ratified reinstating is a decision this system will not make silently. | CBA product owner with the API owner; decide before a Connector-facing roster screen is expected to carry a "do not contact" control. | A suppress action on the §13 roster surface; and, separately from it, any answer to OQ-009 about whether a suppression can ever be lifted and on whose say-so. |
| OQ-CBA-040 | Should a Speaker's **decline** feed back into matching, so a professional who has turned an invitation down is ranked lower — or excluded — next time? `CBA-INVITATIONS` records the answer and nothing reads it: `cba_invitation.response_status` is not a scoring input, and no factor in the registry knows the table exists. | Recorded, never scored. A decline is one data point about one event on one date, and the reasons for it — a diary clash, a topic mismatch, a bad month — are invisible to this system. Treating it as a standing signal would quietly demote somebody for being busy in June, and it would do so through a factor nobody approved: ADR-0016's four factors are what a run is scored on, and adding a fifth in response to a workflow event is a scoring change made by the back door. What a Connector sees instead is the batch history, where "they declined the last two" is legible to a person who can also see which two. | CBA product owner with the matching lead; decide before any surface is asked why a previously shortlisted speaker stopped appearing. | Response-aware ranking; an "unavailable" state distinct from a decline; and any measurement of invitation acceptance rate as a matching input rather than as a report. |
| OQ-CBA-041 | Does an invitation **expire**? There is no deadline column in migration `0029`, so an unanswered invitation stays `awaiting_response` indefinitely, and a Connector reading a batch from March sees the same "no answer yet" as one from yesterday. | No expiry, and no invented one. A deadline is a product decision with a number in it — two weeks? until the event? — and a column defaulted to something plausible would start being enforced by whatever read it first. `awaiting_response` is honest about what is known: nobody answered. What it is *not* is a prompt to act, and a Connector chasing a stale invitation today has to notice the date themselves. Note the interaction with OQ-CBA-044: if invitations can expire, "expired" is a fourth response state and must not be spelled like a decline, because a person who never answered has not refused. | CBA product owner with the events owner; decide before a Connector surface is expected to distinguish "waiting" from "too late", and before any reminder is sent. | Invitation expiry; a reminder message (which would be a second template and a second send); and any dashboard that ages invitations rather than listing them. |
| OQ-CBA-042 | Who may see that a professional **declined**? The batch read is `{admin, coordinator}` — customer §13's Speaker Connector — and the Event Host who asked for a speaker cannot read it. §6 step 9 hands them "the confirmed speaker" and says nothing about the ones who said no. | The narrow reading, deliberately. An Event Host learning that three named professionals declined them is a fact about those people's availability and willingness that nobody agreed to share, and §13 gives the tracking surface to the Connector by name and to nobody else — under deny-by-default the absence of a permit is a denial rather than an invitation to guess. The hand-back in §6 step 9 is therefore a *different* artifact from this read, and building it is a later card's work; approximating it by widening this one would ship the declines along with the acceptance. This decision also binds **OQ-CBA-064**, which asks the adjacent question for a student: any student-scoped roster read built there must be assembled from confirmed or appeared speakers and must never be reachable from invitation state, or it would tell a student what this row refuses to tell an Event Host. | CBA product owner with the privacy owner; decide before the confirmed-speaker hand-back is built, since that card has to answer what the Host is told when nobody accepted. | An Event Host-facing view of their request's outcome; and any status on a Speaker Request that reflects invitation responses. |
| OQ-CBA-043 | Should a Speaker see **their own** invitations — the ones they have not answered, and the engagements they accepted? §14 says Speakers should be able to view invitations, accepted engagements, and upcoming events; what `CBA-INVITATIONS` ships is a token link that answers *one* invitation, not a place a person can see their own. | One tokened link per invitation, and no speaker-facing list. A list needs an identity, and a §13 roster contact is not an account — nothing in this platform issues them a credential, which is the same fact that makes the respond route unauthenticated. Building a list keyed by anything weaker than a login would mean either a long-lived token that enumerates a person's history from an email link, or an identity system for people who never asked for one. So §14's first bullet is served narrowly and the other two are not served at all: a Speaker sees the invitation they were sent, and answers it. | CBA product owner with the named privacy owner and whoever owns OQ-CBA-035's opt-in surface; the two want the same identity and should be decided together. | A speaker portal; §14's "accepted engagements" and "upcoming events"; and any notification to a Speaker that is not a reply to something they were already sent. |
| OQ-CBA-044 | May a Speaker **change an answer** they already gave? `record_response` refuses a different second answer — the first stands, and both the connector route and the Speaker's own link decline to overwrite it. | Fail closed until somebody decides. An acceptance is a fact an Event Host may already have acted on by booking a room and telling students who is coming, and letting a later click erase it silently is the failure this refusal exists to prevent. But the honest case is real: a professional who accepted in March and breaks their leg in May has no way to say so, and today the answer is a phone call to a Connector who also cannot change the row. The remedy that does *not* need this question answered is a second invitation to a rescheduled event, which is a new row with its own answer. Note that "changed their mind", "never answered" and "expired" (OQ-CBA-041) are three different states, and none of them may be spelled like the other two. | CBA product owner with the events owner; decide before a real cohort is invited, since a withdrawal after acceptance is ordinary rather than exceptional. | A withdrawal path; an amended answer with its own trail entry; and any surface that shows an Event Host that a confirmed speaker is no longer coming. |
| OQ-CBA-039 | May §19's classification be produced by a **live model**? The classifier behind every `inferred` value is a deterministic fixture that resolves company and title text against the two released taxonomies and proposes nothing it cannot resolve. Customer §19 says "Company + current position/title analyzed" and does not say by what. | The fixture, and a refusal that is not conditional on where the code runs. `build_contact_classifier` raises `ProviderConfigurationError` if a model credential is present under **any** edition, and again if a live adapter is requested at all — the refusal is a property of what has been approved rather than of the deployment, which is why `ALLOW_LIVE_PROVIDERS` is accepted as an argument and explicitly is *not* sufficient to satisfy it. The unanswered part is not technical: which model, on whose credentials, under whose terms, at what per-run cost, and whether a named person's employer and job title may be sent to a third party at all. That is the same question `topic_semantics` records as OQ-CBA-026 about §9's comparison, asked again about §19's classification, and it is sharper here because the payload is a specific person's employment rather than a topic string. Note what does **not** depend on the answer: a model-produced proposal would still be `inferred`, would still name no actor, and would still await review — migration `0028` needs no column added for it. `tests/unit/test_provider_isolation.py` asserts the refusal under every edition and removes `socket` while doing so, because a classifier that grew a lookup would be a customer §20 scope violation before it was a provider one. | CBA product owner with the named privacy owner and the provider owner; decide before any classification is expected to be better than the two taxonomies' own vocabulary can make it, and before any credential is bound in any environment. | A model-backed classifier; a confidence score, which the fixture deliberately does not produce; and any classification of a company or title the released taxonomies do not name. |
| OQ-CBA-045 | Does an inferred proposal **expire**? A contact imported and never reviewed sits in the `inferred` arm indefinitely — permanently match-ineligible under §19's gate, and permanently invisible unless somebody opens the roster. Migration `0028` added no `review_due_at`, and nothing ages, reminds, or escalates. | No expiry, and no invented deadline. A review window is a product decision with a number in it — a week? until the next event? — and a column defaulted to something plausible would start being enforced by whatever read it first, which is migration `0012`'s refusal to invent a `board_role` vocabulary applied to a duration. What the system does instead is honest rather than silent: `match_ineligibility_reason` returns `{axis}_classification_awaiting_review` rather than a bare false, so a roster screen can distinguish a contact waiting on a human from one nobody could classify, and the two call for different acts. Note the interaction with OQ-CBA-046: if unreviewed proposals are to be worked through a queue, that queue is the surface that question is about, and the two should be decided together rather than one implying the other. | CBA product owner with the Speaker Connector who would own the queue; decide before a pilot cohort is imported at a volume nobody can review in one sitting. | A review deadline; a work queue or reminder for unreviewed classifications; and any report that ages a roster rather than listing it. |
| OQ-CBA-046 | Should an **unclassifiable contact raise a `review_item` of its own**? Migration `0024` handed OQ-CBA-010 — where a quarantined classification lives — to `CBA-IMPORT-CLASSIFY`, and the schema half is answered by adding no column: an unresolvable company or title produces no proposal, and the raw text is already stored in `speaker_profile.company` / `.title` where a reviewer reads it. The surface half is what remains: today a contact nobody could classify is discoverable only by opening the roster and noticing an empty classification. | Nothing additional is raised, and the absence is deliberate rather than deferred by accident. A `review_item` is an *import row* awaiting a decision, and the import row has already been decided — accepted — by the time a classification is proposed; raising a second one against the same row would make one row decidable twice and give `ck_review_item_decision_evidence` two meanings. What keeps this from being a silent drop is the gate: an unclassified contact is match-**ineligible**, reported as `{axis}_classification_missing`, so it cannot quietly enter matching while nobody notices it. It is invisible, not admitted. Do not answer this by making the import fail on an unrecognized code either — `docs/pilot-data/columns.yaml` already ruled that an unrecognized value is a review item's problem and not the batch's. | CBA product owner with the Speaker Connector; decide alongside OQ-CBA-045, since a queue of unreviewed proposals and a queue of unclassifiable contacts are plausibly one screen. | A work queue for contacts the classifier could not read; any surface that counts them; and a decision on whether they differ, to a Connector, from contacts carrying a proposal nobody has checked. |
| OQ-CBA-047 | Who owns **request-level idempotency** for `POST /v1/units/{unit_id}/cba/contacts`? Migration `0030` made a speaker contact's id opaque, and with the name-derived key gone the create route no longer collapses a repeat of the same request into one row: a double-clicked form adds the same person twice. | **Answered 6 September 2026 — the create takes an optional `Idempotency-Key`, routed through the existing substrate.** The interim default was right that nothing should be invented at this layer, and nothing was: `POST /v1/units/{unit_id}/speaker-contacts` reserves through `IdempotencyRepository.reserve` and `fingerprint_request` in `smartmatch_persistence.idempotency`, under `uq_idempotency_scope`'s `(tenant_id, command_type, idempotency_key)` and the command type `speaker_contact.create`. No second scheme was grown inside the router. **The key is optional**: a create without one behaves exactly as it did, duplicate hint and all, because §13's form posts without one and requiring a header would turn every existing caller into a `400` — `submit_command` and `redrive.py` require theirs because they start durable background work, and this route does not. Same key with the same body is a replay answered with the original `201` and the original contact; same key with a different body is `409 idempotency_key_reused`; two different keys with identical bodies create **two** contacts, because two real people can share a name. **The key is taken off the request and never off the name**: no uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` in any form, no application-level refusal, no `409` on a name, and no name-based deduplication anywhere (OQ-CBA-017, OQ-CBA-021). The old behavior was never idempotency; this is. Still no merge surface — a key stops a second row from being created and does nothing about two rows that already exist (OQ-CBA-049). | Answered. Decided by Danny Tran, program owner of record. The interim default named the substrate and deferred only the surface question — whether the header exists and whether it is required — and both are settled above. | An idempotency key on the create route; any client-side retry that must not double-write; and a defensible answer to "I clicked once, why are there two of him". |
| OQ-CBA-048 | The **import side of a speaker's identity is still name-derived**. Migration `0030` re-keyed §13's contacts to opaque ids, but the import path in `smartmatch_domain.cba_contacts` still derives a professional's id from the folded name, on purpose: the same import row seen twice must resolve to one professional, and import has no edit surface through which a rename could make the derivation stale. §13 has one. So a contact imported under the synthetic scheme and then renamed through §13 can fork on a re-import. | Leave the two schemes diverged and state the residual rather than re-keying the importer. Changing it would re-key `tools/generate_pilot_dataset.py`'s fixtures and `services/api/smartmatch_api/pipeline_provisioning.py`, neither of which OQ-CBA-017's decision names, and a half-migrated identity scheme is worse than two honest ones. Do not close this by adding a name-uniqueness constraint on the import side either — OQ-CBA-021 forbids that shape everywhere, and it would reintroduce exactly what the re-key removed. | CBA product owner with the data owner; decide before the same cohort is imported twice with §13 edits in between — that is the sequence that forks a row. | A single identity scheme across import and manual entry; re-import of an edited roster without forking; and retirement of the folded-name derivation in `cba_contacts`. |
| OQ-CBA-049 | Should the **edit route hint at same-name contacts** the way create does? `create` returns `same_name` — the contacts already here under this name — and `update` deliberately returns nothing, so renaming somebody *into* an existing name is the same hazard from the other direction with no signal at all. | **Answered 6 September 2026 — the edit hints, under its own field name.** `PATCH /v1/units/{unit_id}/speaker-contacts/{professional_id}` returns `name_now_shared_with` and `name_now_shared_with_truncated`, mirroring the create's cap (`MAX_SAME_NAME_HINTS`), its one-past-the-cap read and its truncation flag. The field name is deliberately **not** the create's `same_name_contacts`, because the two sentences differ: a create says *you may be about to duplicate somebody*, an edit says *you may have just collided with somebody*. It fires only when the edit moves the name onto one another contact already holds — a no-op edit, an edit that leaves the name alone, and one that only re-cases or re-spaces it all report nothing — and the contact being edited is excluded from its own answer in SQL (`list_same_name(exclude_professional_id=...)`). Still a hint and never a refusal: the `UPDATE` is committed before anybody reads the field, the status stays `200`, and **no uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` was added in any form** (OQ-CBA-021). What was **not** built stands as the interim argument stated it: no merge surface, no `duplicate_of` pointer, no disambiguator column, and no count that means more than *rows sharing a name*. | Answered. Decided by Danny Tran, program owner of record. The surface decision the interim default was waiting on is the field name and the trigger, both settled above. | A hint on the edit route; any merge or "same person" affordance; and any count of duplicates that means more than "rows sharing a name". |
| OQ-CBA-050 | Should the duplicate hint match on **name similarity** rather than exact folded equality? Today `"Dana Ryes"` and `"Dana Reyes"` are one letter apart, are two different strings, and the hint says nothing about them. | **Answered 6 September 2026 — exact folded match only, as the interim default stood.** Both hints — the create's `same_name_contacts` and the edit's `name_now_shared_with` (OQ-CBA-049) — compare `lower(btrim(full_name))` in SQL against `folded_contact_name`'s `strip().casefold()`, and nothing else. No similarity algorithm, no cutoff, no cross-unit rule: `"Dana Ryes"` and `"Dana Reyes"` remain two names and neither route says anything about the pair. The reasoning is unchanged — a hint that guessed at near-misses would fire constantly and train a Connector to dismiss the true positives, and each knob a similarity match needs is a product decision rather than a query detail. **Revisitable**, and this is the condition: when a roster is large enough to measure a real false-positive rate on, not before. Reopening it means a new OQ with that measurement in hand. | Answered. Decided by Danny Tran, program owner of record, alongside OQ-CBA-049 — one message design, and this is the matching rule under both of its triggers. | Fuzzy duplicate detection; any "possible duplicates" report; and a deduplication pass over an existing roster. |
| OQ-CBA-051 | What persisted evidence proves that a particular speaker appeared at a particular event? `student_speaker_feedback` can prove that the student attended the event, but the current schema has no speaker-to-event appearance relation; `cba_invitation_batch.event_name` is free text and cannot supply one. | Require the speaker to be on the feedback unit's roster, and state the limitation. Do not claim roster membership proves an appearance and do not derive the speaker from a name. | CBA product owner with the events and data owners; decide before eligibility must distinguish speakers on the same unit roster who did and did not appear at one event. | A speaker-appearance relation and fully event-specific feedback eligibility. |
| OQ-CBA-052 | May feedback remain editable and withdrawable when an event's `time_precision` is `unresolved`, so no seven-day cutoff can be computed? | Keep the edit window state explicitly `unknown` and permit change. Locking would assert that an unnameable deadline has passed and would prevent a student retracting their words; no fabricated date or `locked_at` value is stored. | CBA product owner with the privacy owner; decide before unresolved-date events are used for feedback in a real cohort. | A final fail-open or fail-closed policy for unresolved event dates. |
| OQ-CBA-053 | Should a student's rating of a speaker influence later matching? | No. Student speaker feedback is an event outcome and remains separate from coordinator match-outcome feedback; no rating is a scoring input and no factor reads this table. | CBA product owner with the matching lead; decide only before proposing feedback-derived ranking or exclusion. | Any rating-derived matching factor, registry/version change, or explanation. |
| OQ-CBA-054 | May a Speaker Connector see students' free-text comments, and if so under what privacy threshold or presentation? | Do not expose comments. The Connector route returns only the thresholded mean and count; it offers no individual-rating or transcript endpoint. | CBA product owner with the privacy owner; decide before any Connector-facing response model gains comments or individual rows. | Connector comment review, moderation, retention presentation, and any comment-specific suppression rule. |
| OQ-CBA-059 | Who assembles the **match-run pool**? `POST /v1/units/{unit_id}/match-runs` now takes `candidate_subject_ids` — the caller names who to consider, and the server reads what they are like. The alternative is that the server assembles the pool too: every match-eligible `speaker_profile` this unit owns, with no candidate list on the body at all. | The caller names them. That is what OQ-CBA-031 shipped, and it is the smaller claim: naming a subject asserts nothing about them, so the trust defect the card fixed does not reopen, and a Connector who wants to score a specific handful can. What it does **not** do is answer "who should be in this run", which is a product question with a screen behind it — a roster with checkboxes, or a button that means "everyone eligible". Note the interaction with `MAX_CANDIDATES` (200): a server-assembled pool would have to decide what a 201st eligible contact means, and truncating a roster silently is how a speaker stops being considered without anybody choosing that. | CBA product owner with the Speaker Connector who would use the screen; decide alongside OQ-CBA-045 and OQ-CBA-046, since a roster with a review queue and a roster with a "match all eligible" button are the same screen. | A one-click run over a whole roster; any count of "speakers considered" that means more than "speakers this caller named". |
| OQ-CBA-060 | May a **parent unit** match on a child unit's roster? `assemble_cba_pool` scopes `speaker_profile` by `owning_unit_id` equality, so a coordinator authorized at `iawest` cannot score a speaker owned by `iawest.matching` even though `smartmatch_authz` would let them read that subtree. | Equality, which is the fail-closed reading of an unanswered question. A profile owned by another unit is reported `speaker_profile_not_found` rather than refused, so the answer is the same one an unknown id gets and reveals nothing. Widening this to subtree containment is a one-line change and deliberately not made here: a roster is a list of named people a specific Connector is accountable for, and quietly making a parent unit's runs reach every child's contacts is a data-visibility decision, not a query-scope convenience. Note that the *run* is already filed under the authorized unit, so widening the pool without widening that would file a run under one unit about people another unit owns. | CBA product owner with the org-model owner; decide before any tenant runs more than one unit with speakers in both. | Cross-unit match runs; a shared roster; and any "all speakers in my organization" surface. |
| OQ-CBA-061 | A speaker with **topic text on file scores worse than one with none**. Customer §9 gives an observed absence a neutral policy value, so a profile with no topic text is scorable; a profile *with* topic text is compared through `SemanticTopicProvider`, which under OQ-CBA-026 is a playback fixture holding no recording for real data — so the comparison is unknown, the composite is unknown, and the best-documented speaker is excluded from every shortlist. | Nothing is worked around, and this is recorded rather than patched. Both halves are correct on their own terms: an unrecorded comparison is genuinely unknown (a lexical fallback would put an untrue provenance claim in stored data permanently — OQ-CBA-026 says so explicitly), and §9's neutral is a stated customer policy. The perverse ordering is what those two produce *together* while no approved semantic model exists, and it is visible rather than hidden: the excluded speaker is reported unscorable with the factor and the basis that made it so. Do **not** answer this by widening the §9 neutral to cover an unevaluable comparison — the customer stated a policy for an absence we observed, not for a comparison we could not make, and applying it here would file "we could not check" as "we checked and it was average". | CBA product owner with the matching lead; decide with OQ-CBA-026, since answering that one dissolves this one. If OQ-CBA-026 stays open through a pilot, this needs its own answer first — a demo where filling in a profile removes a speaker from the shortlist is worse than one where nobody filled anything in. | Any pilot whose speakers have real topic text; and any instruction to a Connector to complete profile topic fields. |
| OQ-CBA-062 | `smartmatch_domain.factors.cba_semantic_topic.TopicComparison` declares its members as **settable** attributes, so `smartmatch_providers.topic_semantics.TopicSimilarity` — a frozen dataclass — does not structurally satisfy it under strict mypy, even though it carries every member and `isinstance` accepts it. | A documented `cast` at the single call site in `smartmatch_api.routers.match_runs._topic_provider`, and nothing else. This surfaced only when the first *strictly typed* call site appeared: the golden tests pass the fixture directly and `tests.*` is excluded from strict typing, so the protocol had never been checked against a real implementation until OQ-CBA-031. The fix is to declare the protocol's members read-only, which is a change to `smartmatch_domain` and belongs with whoever owns that layering contract rather than with the router that tripped over it. Do not answer it by unfreezing `TopicSimilarity` — an immutable comparison result is the property that keeps a stored provenance claim from being edited after the fact. | Matching lead with the domain owner; decide before a second typed call site is written, since the second `cast` is where a workaround becomes a convention. | Removing the cast; and any adapter that implements `TopicComparison` with a mutable type because the protocol invited one. |
| OQ-CBA-063 | How should a speaker's **city name** resolve to a coordinate, when `speaker_profile` stores `location_city` with **no state column**? OQ-CBA-024 shipped the ZIP half; a city is the other arm of §10's "City or ZIP code is sufficient for this phase", and today a speaker with a city and no postal code is `unknown` — correctly, but permanently. | **Open. No city is resolved and none is guessed.** "Pomona" is a city in California and a village in New York, and there are Ontarios, Riversides and Glendales in several states; a table keyed on the folded name alone would place every one of them in California and return a distance indistinguishable from a measured one — the ADR-0011 defect exactly. Nor is "assume California" a safe default: it is silently right for most of a CPP roster and silently wrong for the speaker who moved, and nothing in the stored score would say which. Three candidate answers, each with a different cost and none an implementation detail: **(a)** add a state column and require it on import, which changes the §13 contact form and the import contract and needs a backfill for existing rows; **(b)** resolve a city only when the name is unambiguous *within California* and leave every ambiguous one unknown, which is honest but leaves a Connector unable to see why one city resolved and another did not; **(c)** ask the Connector for a ZIP instead, which makes the resolvable field the required one — the smallest change to the system and the largest to the person doing the typing. Note the interaction with OQ-CBA-024's scope: a city table would face the same California-only question, and "Portland" would be the first argument about it. | CBA product owner with the data owner and the Speaker Connector; decide before a pilot roster is expected to be matched on physical events with cities rather than ZIPs recorded, since until then such a speaker is unscorable under `cba-physical-1`. | City-only proximity; any "assume in-state" resolution rule; a `location_state` column and its import contract; and any instruction to a Connector that a city alone is enough for a physical run. |
| OQ-CBA-064 | How does a student **discover a speaker to rate for the first time**? OQ-CBA-003 shipped the write, and `POST /v1/units/{unit_id}/student/events/{event_id}/speakers/{speaker_id}/feedback` takes the speaker as a path parameter — so a client must already hold a speaker id before it can call it. **No student-authorized route returns one.** Every roster read is `{admin, coordinator}`, and the only speaker id a student is ever told is one already attached to a rating they made. A student can therefore amend and withdraw, and has no honest way to submit a first rating. | **Open. No student-scoped roster read is added and no speaker id is invented.** What is true today, exactly. The student surface is five routes across two routers: `GET .../student/events` and `GET .../student/agenda` (`routers/student_events.py`, `_STUDENT_EVENT_ROLES = {"student"}`), the registration `POST`/`DELETE` pair (`_STUDENT_REGISTRATION_ROLES = {"student"}`), and the feedback submit/withdraw/list trio (`routers/student_speaker_feedback.py`, `_STUDENT_FEEDBACK_WRITE_ROLES` and `_STUDENT_FEEDBACK_READ_ROLES`, both `{"student"}`). **None of the event reads carries a speaker.** `StudentEventSummary` in `contracts/openapi/smartmatch.json` has eleven properties — `id`, `title`, `description`, `time`, `is_virtual`, `location_city`, `location_postal_code`, `tags`, `registration`, `on_my_agenda`, `calendar` — and no speaker field of any kind. The one student route that names speakers, `GET .../student/events/{event_id}/speaker-feedback`, is scoped by `principal.user_id` and returns the caller's **own** `StudentFeedbackView` rows: it reports a `speaker_professional_id` the student already has, which is exactly why amend and withdraw work and first submission does not. On the other side of the boundary, `GET .../units/{unit_id}/speaker-contacts` (`routers/cba_contacts.py`, `_SPEAKER_CONTACT_ROLES = {"admin", "coordinator"}`) and `GET .../units/{unit_id}/cba/confirmed-speakers` (`routers/cba_handoff.py`, `_HANDOFF_ROLES = {"admin", "coordinator"}`) both refuse a student — a `403` from `assert_allowed` on a required-roles check, not a `404`. So does the aggregate `GET .../units/{unit_id}/speakers/{speaker_id}/feedback-summary` (`_SPEAKER_FEEDBACK_SUMMARY_ROLES = {"admin", "coordinator"}`), and it needs a speaker id of its own in any case. The server does hold the fact the student is missing, and refuses to volunteer it: `_require_eligible_and_open` in `routers/student_speaker_feedback.py` asks `smartmatch_persistence.student_speaker_feedback` whether the named speaker is on the unit's roster and whether this student attended, and answers `404 speaker_contact_not_found` or `403 student_feedback_not_eligible` — a **yes/no about one id the caller supplied**, never a listing. **Do not close this by asking a student to type a UUID, and do not close it by pointing a student client at a route the server refuses.** The first is caller-selected identity in a new spelling, which is the defect the feedback write was built to exclude (`student_id` comes from the verified principal and no route accepts one in a body or a path); the second ships a client that is broken by design. Three candidate answers, none adopted, each a different disclosure. **(a) A student-scoped roster read, limited to events the student has an attendance record for.** Smallest surface a picker needs, and it says the most: it tells a student the set of professionals on their unit's roster associated with an event they attended, which is a list of named people and the fact of their involvement. **(b) Derive the list from the student's own attendance rows.** `routers/student_events.py::_attended_event_ids` already reads `attendance_record` keyed on the verified principal, so the student's half is established and self-scoped; what is missing is the other half — the schema has **no speaker-to-event appearance relation at all**, which is OQ-CBA-051, and roster membership is the stand-in the write already uses. So (b) cannot be built more narrowly than (a) until OQ-CBA-051 is answered: today "the speakers at the event I attended" can only be computed as "the speakers on the unit roster", which is (a) wearing a narrower name. **(c) Surface speakers only through an already-authorized student surface** — a speaker field on `StudentEventSummary` or on the agenda — which moves the question rather than answering it: the same names reach the same student, now on a route that also serves events they merely browsed rather than attended, so the disclosure is wider than (a) unless the field is suppressed for non-attended events. **This is a privacy question before it is a UI question, and it touches a settled one.** A roster read tells a student who was *invited*, and OQ-CBA-042 decided that even an Event Host does not learn who declined — the tracking surface belongs to the Speaker Connector by name and to nobody else. Any student-scoped read must therefore be built from **confirmed or appeared** speakers and must never be reachable from invitation state; a read that leaked a declined professional would answer, for a student, a question this register already refused an Event Host. Note also what this is **not** about: OQ-CBA-053 keeps ratings out of the match engine, so nothing here is a scoring input, and OQ-CBA-003's Connector reads stay aggregate-only in both directions — a picker owes a student a name to rate, never another student's rating. | CBA product owner with the API owner; and with the privacy owner on the disclosure, since OQ-CBA-042 is the neighbouring decision. Decide before any student-facing feedback surface is expected to accept a **first** rating, and alongside OQ-CBA-051, which determines whether candidate (b) can ever be narrower than candidate (a). | First-time student feedback submission through a UI; any speaker picker on a student surface; and any student-visible statement of who spoke at an event. |

### OQ-CBA-004 decision record requirements

The decision must cite [`docs/architecture/decisions/ADR-0011-accountable-numbers.md`](../../architecture/decisions/ADR-0011-accountable-numbers.md) and answer both cases separately:

1. Missing Topic evidence: whether “neutral” is an explicit customer policy value with distinct provenance or still an unknown value excluded from scoring.
2. Virtual events: whether excluding Proximity is a conditional factor registry, an approved normalization rule, or another versioned policy.

The approved artifact must define serialization, UI labeling, match-run pins/fingerprints, and golden cases. Approval must precede edits to [`python/smartmatch_domain/smartmatch_domain/factor_registry.py`](../../../python/smartmatch_domain/smartmatch_domain/factor_registry.py).

#### Drafting status, and what the owner must still approve

[`ADR-0016-cba-scoring-policy.md`](../../architecture/decisions/ADR-0016-cba-scoring-policy.md) drafts every one of those fields. Its status is **Proposed, not Accepted**, and it records no decision date and no signature, because the named owner has not decided. A drafted proposal is not an approval, and the question below stays **Open — awaiting owner decision**:

| # | ADR-0016 proposal | What Danny Tran must approve, amend, or reject |
|---|---|---|
| 1 | Third evidence state | That `policy_neutral` (an observed absence of usable topic evidence) is a distinct state from `unknown` (evidence that could not be evaluated), and that only the latter makes the composite unknown. |
| 2 | Neutral value and provenance | `CBA_NEUTRAL_TOPIC_VALUE` = `0.50` (or `0.40`, or another value), plus the policy id, policy version, and the `basis` wording a Speaker Connector reads. |
| 3 | Proximity band sub-scores | `1.00` / `0.60` / `0.20`, as a step function, and that exactly 25 miles is Mid and exactly 75 miles is Far (lower-inclusive, upper-exclusive). |
| 4 | Missing address | That an unknown distance is `unknown` and not the far band, accepting that such candidates are unscorable and sort last. |
| 5 | Virtual events as a scoring mode | That excluding Proximity is a named, pinned scoring mode (`cba-physical-1` / `cba-virtual-1`) rather than a conditional registry. |
| 6 | Virtual redistribution formula | Proportional renormalization (`0.428571` / `0.357143` / `0.214286`) **or** a fixed table such as `0.40` / `0.35` / `0.25`. |
| 7 | Serialization | That a stored score states which of its factors were policy values, via `policy_neutral_factor_keys`, `policy_id`, `policy_version`, `scoring_mode`. |
| 8 | UI labels | The seven coordinator-facing strings, especially the neutral-Topic label. |
| 9 | `REGISTRY_VERSION` pin policy | That adopting 1–8 is a major bump to `2.0.0-approved-oq-cba-004`, and that runs pinned to `1.1.1-approved-g1-m6j` are retained but never compared across the bump. |

Until every row above is answered, `factor_registry.py` must not change, no CBA golden case may be written, and `tests/unit/test_cba_scoring_decision_artifact.py`'s approval assertions stay `xfail(strict=True)` with OQ-CBA-004 named in the reason.

## Closed by ADR-0016, accepted 5 September 2026

Owner: Danny Tran, Development Lead / program owner of record. All ten of ADR-0016's
numbered proposals were put to the owner and approved as drafted, with no amendments.
The rows above keep their full argument; this table is the closure of record.

| OQ | Status | Decision |
|---|---|---|
| OQ-CBA-001 | **Closed 5 September 2026** | Virtual events drop Proximity through a named pinned scoring mode (`cba-virtual-1`), never a second registry. The freed 30% is redistributed by proportional renormalization — Industry `0.428571`, Role `0.357143`, Topic `0.214286` — matching customer §11's "approximately 42.86 / 35.71 / 21.43". ADR-0016 Proposals 5, 6. |
| OQ-CBA-002 | **Closed 5 September 2026** | Proximity is a step function on raw miles, no interpolation inside a band: Near `0 ≤ d < 25` → `1.00`, Mid `25 ≤ d < 75` → `0.60`, Far `75 ≤ d` → `0.20`. Lower-inclusive and upper-exclusive throughout, so exactly 25 is Mid and exactly 75 is Far, compared on the raw float with no pre-rounding. Far is `0.20` and not `0.00`, so a measured zero stays a real claim. No city and no ZIP is `unknown`, explicitly **not** Far. ADR-0016 Proposals 3, 4, 5. |
| OQ-CBA-004 | **Closed 5 September 2026** | ADR-0011 is extended, not amended: `policy_neutral` is a customer-directed policy value distinct from `unknown`, and only `unknown` makes a composite unscorable — weights are never re-spread per candidate. Missing topic evidence contributes `0.50` under the named versioned constant `CBA_NEUTRAL_TOPIC_VALUE` (`cba-neutral-topic 1.0.0`), recorded on every score that used it. `REGISTRY_VERSION` moves to `2.0.0-approved-oq-cba-004`; runs pinned to `1.1.1-approved-g1-m6j` stay readable but are never compared across the bump, and scoring mode pins separately from registry version. ADR-0016 Proposals 1, 2, 7, 9. |

Acceptance licenses implementation of these values. It does not license substituting
different ones: a later card that wants another neutral value, band edge, or
redistribution must amend ADR-0016 and have the amendment approved.
## Decisions taken 2026-09-05

Owner: Danny Tran, Development Lead / program owner of record. Recorded from a working
session against the rows above. Each row's interim behavior stays readable as the argument
that produced the decision; this section states what is now settled.

| OQ | Decision | Obligation |
|---|---|---|
| OQ-CBA-015 | **Decided — keep the field, report the discard.** `contact_email` stays on §13's create/update form, is never persisted, and the discard is reported in `withheld_fields`. | None. This is the shipped behavior. Still to be confirmed jointly with the privacy owner's OQ-CBA-011 answer; no answer here makes an address send-eligible, because `smartmatch_domain.consent` admits four approved sources and a form entry is not one. |
| OQ-CBA-016 | **Decided — provenance-only rule ratified.** `board_role` records how a row was created, never what a person does. `cba_speaker_contact` stands. | Never render `board_role` to a user, never filter a query on it, and never add sibling values one card at a time. A speaker's role category is `speaker_profile.primary_role_code`, held to customer §8's closed list. A future card that needs to tell row kinds apart must ratify a vocabulary first, not extend this column by habit. |
| OQ-CBA-017 | **Decided — identity becomes opaque.** `speaker_profile` stops deriving its key from `uuid5(namespace, "tenant:unit:folded_name")`. The id becomes opaque and `full_name` becomes an ordinary column. | New work, queued behind migration `0026`. Closes the rename fork: today an edit that changes a name does not move the derived key, so a later create under the corrected name silently produces a second contact for one person. The `409 speaker_contact_name_already_used` refusal is removed with it — same-name creates become the legitimately distinct people they are. Two consequences follow and are **not** settled by this decision: whether existing name-derived ids are re-keyed with their FK references updated or left alongside opaque ids, and how a Connector is warned they may be creating a second record for someone already present. The 409 was doing crude duplicate prevention as a side effect; removing it leaves that job unowned. Raised as OQ-CBA-021. |
| OQ-CBA-018 | **Decided — cancellation is a status transition.** `event_registration.status` flips to `cancelled` and the row is retained. | Never model a cancellation as a `DELETE`: "cancelled" and "never registered" must stay distinguishable, and a coordinator will want to see who withdrew. Re-registering is an idempotent flip on the same row rather than a new one. Capacity and waitlist columns are deliberately **not** committed to migration `0026` — that requirement is unconfirmed and a guessed waitlist is a permanent default nobody approved. |

## Decision taken 2026-09-06 — OQ-CBA-003

Owner: Danny Tran, program owner of record. The decision is the minimum contract
implemented by `CBA-STUDENT-FEEDBACK`; anything wider returns to this register.

| Concern | Decision | Obligation |
|---|---|---|
| Scale and fields | One required overall integer rating, `1..5`, and one optional free-text comment of at most 2,000 characters. Blank comments normalize to absence. No sub-criteria are approved. | The domain and database both enforce the scale and comment shape. An unrated speaker has no numeric value; absence never becomes zero. |
| Eligibility | Only the verified student may write their own row, and only for an event with an attendance record for that student. The selected speaker must be on the unit roster. | `student_id` comes from the verified principal, never a request field. Attendance is also a composite foreign key. Roster membership is only the nearest available speaker evidence; OQ-CBA-051 records the missing event appearance relation. |
| Edit and withdrawal | A rating is editable and withdrawable until seven days after the event's known end, or start when no end was stated. A withdrawal is a status transition on the same row and clears both rating and comment; resubmission reuses that row. | At the closing instant the window is closed. An unresolved event date produces an explicit `unknown` window and currently permits change, pending OQ-CBA-052. |
| Attribution and Connector privacy | Store the student's identity for self-scope, de-duplication, withdrawal, and abuse tracing. Do not expose it to the Connector. | Connector responses contain no student identifier, individual ratings, timestamps, or comments. OQ-CBA-054 owns any future comment surface. |
| Aggregation | Connector reads publish the arithmetic mean and response count only when at least three submitted ratings exist. Below three, including zero, both numbers are `null` and the response says `not enough responses yet`. | Withdrawn rows do not count. The threshold is evaluated on each read, so a withdrawal can return an aggregate to the suppressed state. |
| Matching boundary | Student speaker feedback does not feed the match engine. | Keep it separate from coordinator match-outcome feedback unless OQ-CBA-053 is decided and the scoring policy/version is amended. |

## Decision taken 2026-09-06 — OQ-CBA-008

Owner: Danny Tran, program owner of record.

| OQ | Decision | Obligation |
|---|---|---|
| OQ-CBA-008 | **Decided — provenance, no history.** A classification records how its **current** value was set and nothing about what it was before: `{axis}_classification_source` (`inferred` \| `human`), `{axis}_classified_by_user_id`, `{axis}_classified_at`, added to `speaker_profile` by migration `0028`. A previous-value table, revision rows, and an `industry_source` free-text vocabulary are all explicitly **declined**. The question provenance answers is "can I trust this?", not "what changed?" — a code the pipeline proposed and a code a Connector chose after reading the person's own account of their work are the same characters in the same column, and customer §19's review gate between them is invisible without a source. The previous value is not evidence about the current one. | Never add a `speaker_profile_classification_transition` table, revision rows, or previous-value columns under this decision; a later card wanting one needs its own stated audit requirement. Never spell a corrected value as a third source — a correction is `human`, indistinguishable from a value typed at create time, because both are a person's judgment and that is the only property §19's step five turns on. An actor may appear **only** beside `human`, and `ck_speaker_profile_{axis}_provenance` enforces it rather than trusting a convention. |

**Left standing on their interim behavior, no decision required.** OQ-CBA-019: the student
surfaces stay `{student}` only; a Connector reads `events.list` and
`engagement.attendance_summary.read` instead, and widening this role set is not the way to
build a Connector-facing view. OQ-CBA-020: the month calendar's cells stay inert until
registration exists, because the action a day would offer is registration and a day that
opens a disabled control is worse than a day that opens nothing.


### Connector weights UI — deferred by `CBA-MATCH-WEIGHTS`, 6 September 2026

**No frontend surface was built for the matching weights, and this is the written
deferral the card's `deferral_policy` allows** ("complex admin UI and autonomous
feedback tuning are deferred"). What exists is the API:
`GET`/`PATCH /v1/units/{unit_id}/matching-weights`, admin/coordinator, unit
scoped, with the policy-matrix rows to prove it.

Three reasons, in order of weight:

1. **Nothing is hidden by the absence.** A Connector's weighting is readable and
   changeable through the documented contract, and the `GET` already returns the
   *effective* weights per scoring mode — the number a screen would have had to
   compute. A later UI renders what the route returns; it does not need anything
   this card would have had to design first.
2. **A half-built panel is the failure mode this repository already named.**
   `docs/plans/frontend-broken-buttons.md` treats a control with nothing behind
   it as a defect rather than a smaller product, and a weights form is exactly the
   shape that goes wrong that way: a slider that looks live, saves nothing, and
   tells nobody. The honest states here are "no surface" and "a surface that
   round-trips, refuses, and shows the version it is editing" — and the second is
   more than a minimal panel.
3. **The role question is already settled and does not wait on a screen.** A UI
   label grants nothing: `_authorize_matching_weights` is the permit, a student
   and a volunteer are refused both routes server-side, and
   `tests/authz/test_policy_matrix.py` executes all fourteen shapes against the
   real authorizer. Shipping the UI later cannot widen that.

**What a later card owes.** The panel belongs on the Connector surface beside the
shortlist, must send the `expected_version` it read (or accept the 409 it will
otherwise get), must render `ignored_factor_keys` rather than dropping them, and
must not print a registry default as a placeholder — the response's `modes` is
where an effective weight comes from, and a placeholder typed into a form would
be the duplicated default this whole card exists to prevent. See OQ-CBA-033 for
whether that panel presents the two event shapes separately.

## Decision taken 2026-09-06 — OQ-CBA-032

Owner: Danny Tran (@BrooklynD23), program owner of record and the holder of ongoing weight
governance named by `docs/plans/workshops/g1-workshop-output-worksheet.md` agenda item 4.1.

This decision **narrows** agenda item 4.2 of that worksheet — ratified 2026-09-03, "no
weight change ships without shadow evaluation pass" — for the pilot only. It is recorded
here, alongside the other 6 September decisions, because it is a narrowing and not a
completion. The worksheet is left exactly as ratified and is **not** edited: a ratified
record that quietly acquires a later exception stops being a record of what was ratified.
This section is the exception, and it is scoped to the synthetic pilot.

| Concern | Decision | Obligation |
|---|---|---|
| The gate | **No shadow-evaluation gate is built, and none is faked.** A matching-weight change takes effect for the **next** run submitted, and for no run already recorded. | Do not add an advisory shadow run, a stored evaluation record, or a "validated" flag on a weight set without reopening this. A gate nobody specified is a policy invented in code. |
| What a change cannot move | Nothing already recorded. A `match_run` row stores the weights it scored with, and migration `0018`'s `match_run_reject_mutation()` trigger raises `restrict_violation` on any `UPDATE`. | Never introduce a foreign key from a run to a weight setting, and never recompute a stored run's utilities. A correction is a new run. |
| Attribution and reversal | Every accepted change is versioned and attributed. `match_weight_setting_revision` (migration `0027`) carries `changed_by_user_id` `NOT NULL` and the version it produced, is unique per unit and version, and is itself immutable under `match_weight_setting_revision_is_immutable`. `expected_version` with its `409 matching_weights_stale` in `services/api/smartmatch_api/routers/matching_weights.py` stops one Connector's save from discarding another's. | The audit trail is the compensating control, so it may not be weakened to make a surface simpler. Note `expected_version` is optional: a client that omits it gets last-write-wins, and the revision log records that too. A later UI must send the version it read. |
| Why a shadow run was not built | An advisory shadow run needs a candidate pool, and there is none to take. `MatchRunRequest` carries a `speaker_request_id` and an explicit `candidate_subject_ids` list, so a pool exists only where a live request submitted one; a unit adjusting its weights outside a request has nothing to score. Over synthetic pilot data the result would tell you close to nothing while *looking* like validation, and shipping it would invite the reading that these weights had been validated when they had not. | Do not manufacture a pool — a unit's whole roster, the last request's candidates, a fixture cohort — to give a shadow run something to chew on. Each is a different definition of "candidate pool" and none of them has been decided. |
| What MM-005 actually governs | MM-005's shadow mode is a **different** control, and it is already satisfied: it governs *machine-proposed* adjustments, and `smartmatch_domain.feedback.WeightProposal.requires_approval` is a setterless property on a `@final`, frozen, slotted class, so a proposal can never apply itself. Nothing in this repository consumes those proposals. | Stop citing MM-005 as the source of a gate on a Connector's deliberate change; the source is the G1 worksheet. Note also that the v1.1 Appendix B text MM-005 rests on is not vendored here — `docs/migration/migration-manifest.yaml` records `contract_refs_status: UNVERIFIABLE` for that entry — so nobody in this repository has read it. |
| Re-entry trigger | **Revisit before the first time a unit's weights are adjusted against a live cohort rather than a synthetic one.** That trigger, not the gate, is what this decision writes down. | Whoever runs the first live cohort owns raising it, with the matching lead. Reaching that point without reopening this is the failure this record exists to prevent. |

**Costed at the time of the decision**, so a reopening starts from a number rather than from
scratch: an advisory shadow run is roughly 2–3 days and needs no DDL; a blocking gate with a
stored evaluation record is roughly a week and a migration.

**What this decision does not do.** It does not close the question of whether a weighting
should ever be evaluated before it ships — it says the pilot does not do it, and names when
to ask again. It does not license changing a stored run, widening the two roles that may
write weights, or presenting an unevaluated weighting as an evaluated one on any surface.


## Decision taken 2026-09-06 — OQ-CBA-049 and OQ-CBA-050

Owner: Danny Tran, program owner of record. One message design with two triggers, which
is why the two rows are decided together and recorded in one section.

| OQ | Decision | Obligation |
|---|---|---|
| OQ-CBA-049 | **Decided — the edit hints, and says something the create does not.** The §13 edit route reports `name_now_shared_with` (plus `name_now_shared_with_truncated`) when it moves a contact's name onto a name another contact in the same unit already holds. Deliberately a **different field** from the create's `same_name_contacts`: a create warns *you may be about to duplicate somebody*, an edit warns *you may have just collided with somebody*, and a client holding one under the other's key would be reading the wrong sentence. The trigger is the change, not the state — an edit that leaves the name alone, or only re-cases or re-spaces it, reports nothing, because §13's form posts the whole record on every save and a warning that fires every time is one nobody reads. | Never let either hint become a refusal. There is no `409` on this surface and **no uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` in any form — not in the database, not as an application-level refusal** (OQ-CBA-021, OQ-CBA-017): the write commits and the hint describes the roster afterwards. The contact being edited must never appear in its own hint; the exclusion is a SQL predicate rather than a later filter, so it cannot silently consume one of the cap's rows. No merge surface, no `duplicate_of`, no disambiguator column, no "same person" affordance — recording that two rows are one person remains a product decision nobody has taken. Keep the two field names distinct; collapsing them into one would re-merge two claims this decision separated. |
| OQ-CBA-050 | **Decided — exact folded match only.** Both hints compare a case- and space-insensitive fold and nothing more: `lower(btrim(full_name))` in SQL, `strip().casefold()` in the domain, indexed by `ix_speaker_profile_unit_folded_name`. No similarity scoring, no edit distance, no cutoff, no cross-unit widening. `"Dana Ryes"` and `"Dana Reyes"` are two names. | Never add fuzzy matching, a "possible duplicates" report, or a deduplication pass under this decision. A change to the fold on either side must move the index with it or the read silently stops using it. **Revisitable** on one condition, stated so a later card does not have to guess at it: a roster large enough to measure a real false-positive rate on. Until somebody has that measurement, silence on near-misses is the approved behaviour rather than a gap. |

## Decision taken 2026-09-06 — OQ-CBA-047

Owner: Danny Tran, program owner of record. Decided alongside OQ-CBA-049 and OQ-CBA-050
and recorded separately, because it is a different mechanism: those two are about a
**warning**, and this one is about a **request**. The distinction is the whole decision.
A hint tells a Connector that the name they are typing is already on the roster; it
cannot stop a double click, and it does not repair one. With no merge surface anywhere
in this product — OQ-CBA-049 records that building one is a separate decision nobody has
taken — the failure this closes is not "a duplicate appears" but *two rows for one person
with no way to combine them*.

| OQ | Decision | Obligation |
|---|---|---|
| OQ-CBA-047 | **Decided — the create takes an optional `Idempotency-Key`, through the substrate that already exists.** `POST /v1/units/{unit_id}/speaker-contacts` reserves through `IdempotencyRepository.reserve` and `fingerprint_request` in `smartmatch_persistence.idempotency`, scoped by `uq_idempotency_scope`'s `(tenant_id, command_type, idempotency_key)` under the command type `speaker_contact.create`. Four cases, and all four are contract-tested. **No key** — exactly the behaviour that shipped before, duplicate hint and all; the header is optional and stays optional, because §13's form posts without one and requiring it would turn every existing caller into a `400` (`submit_command` and `redrive.py` require theirs because they start durable background work; this route creates its rows synchronously). **Same key, same body** — a replay, answered with the original `201` and the original contact, no second row. **Same key, different body** — `409 idempotency_key_reused`; answering with the earlier contact would silently discard the request the caller actually made. **Two different keys, identical bodies** — two contacts. The owning unit is hashed into the fingerprint, because the substrate's scope does not include it and a key replayed against a second department would otherwise return the first department's contact. | **The key is taken off the request and never off the name.** Never add a uniqueness constraint on `(tenant_id, owning_unit_id, full_name)` in any form — not in the database, not as an application-level refusal, not as a `409` on a name — and never deduplicate by name anywhere (OQ-CBA-017, OQ-CBA-021). The old scheme was not idempotency: it deduplicated by name, so it stopped working the moment anybody corrected one and it refused two genuinely different people who share a name. The test that guards this is the fourth case above — two different keys with identical bodies must keep producing two contacts. Never make the header required; that breaks every existing caller. Never grow a second idempotency scheme inside the router: if the scope, the honoring window or the replay contract needs to change, it changes in `smartmatch_persistence.idempotency` for every caller at once. A replay's body is **re-derived, not stored** — `idempotency_record` has nowhere to keep a response — so it is exact for the double click it exists to close and is a re-answer, not a snapshot, if the roster moved in between; a stored snapshot would need DDL and a decision of its own. Still no merge surface: a key stops a second row from being created and does nothing whatever about two rows that already exist. |

## Decision taken 2026-09-06 — OQ-CBA-024

Owner: Danny Tran, program owner of record. The steer was taken 5 September 2026 and the
ZIP half was built on 6 September; this section is the closure of record. The row above
keeps its full argument. **The city half is not closed and is not covered here — it is
OQ-CBA-063.**

| OQ | Decision | Obligation |
|---|---|---|
| OQ-CBA-024 | **Decided — a static, offline, versioned ZIP-centroid table, and an absent ZIP is `unknown` and never a guess.** `python/smartmatch_domain/smartmatch_domain/zcta_centroids.py` is generated at build time by `tools/generate_zcta_centroids.py` from the US Census 2023 national ZCTA Gazetteer and the 2020 ZCTA-to-County relationship file — both public domain, both pinned by SHA-256. Every coordinate is copied from the Gazetteer unmodified; none was typed, rounded, interpolated, or estimated by hand. Scope is **California only**, 1,808 ZCTAs (any part in a state-FIPS `06` county), because a national table is a wider claim than anybody made. `smartmatch_api.zip_proximity.resolve_distance_from_campus` reads it at the API/evidence layer; `factors/proximity.py` still resolves nothing itself, still takes an already-resolved `distance_miles`, and records the provenance `zcta-centroid-2023` in the score's `basis` under an estimate label naming the ZIP-centroid approximation (ADR-0011). There is no network call and no geocoding provider anywhere on this path. | **A blank, malformed, or non-Californian ZIP resolves to `None`, which is `unknown`** — never a nearest neighbour, never a state centroid, never the Far band (ADR-0016 Proposal 4). Such a candidate is excluded from the match-run pool and **reported**, not entered at `0.0` and ranked last. Never let the table acquire a hand-entered coordinate: it is generated, and a coordinate that did not come from the pinned Gazetteer does not belong in it. Widening beyond California, moving the Gazetteer vintage, or resolving a city name (OQ-CBA-063) each re-open this row and none of them is an implementation detail. Band thresholds and factor weights stay in `factor_registry` and never migrate into the resolver. |


## CBA-gated capabilities

| Capability | CBA disposition | Current implementation surfaces | Gate/re-entry condition |
|---|---|---|---|
| Rewards and points refinements | **Preserve truthful, server-backed rewards/points capability.** Customer §4 explicitly says “Rewards / points — Keep”; §25 makes only refinements and possible career-readiness wording P2. Gate a route, control, or claim only when current evidence shows that specific surface is chapter-specific, unfunded, incomplete, or fake-success. Do not blanket-hide the rewards API or Student Rewards page merely because chapter membership is removed. | [`services/api/smartmatch_api/routers/rewards.py`](../../../services/api/smartmatch_api/routers/rewards.py), [`python/smartmatch_domain/smartmatch_domain/rewards.py`](../../../python/smartmatch_domain/smartmatch_domain/rewards.py), [`python/smartmatch_persistence/smartmatch_persistence/rewards.py`](../../../python/smartmatch_persistence/smartmatch_persistence/rewards.py), [`apps/web/legacy-frontend/src/app/pages/student/StudentRewards.tsx`](../../../apps/web/legacy-frontend/src/app/pages/student/StudentRewards.tsx), [`docs/decisions/d6-rewards-budget-decision-record.md`](../../decisions/d6-rewards-budget-decision-record.md) | P2 may refine CBA wording/career-readiness framing. Any narrow gate must name the defective surface and its re-entry condition; working ledger-backed catalog, balance, and redemption behavior remain in scope. |
| `member_inquiry` pipeline stage | Preserve schema/history and R/Y/G discovery feed; suppress the CBA funnel tile and prohibit new CBA writers. | [`python/smartmatch_domain/smartmatch_domain/pipeline.py`](../../../python/smartmatch_domain/smartmatch_domain/pipeline.py), [`python/smartmatch_persistence/smartmatch_persistence/pipeline.py`](../../../python/smartmatch_persistence/smartmatch_persistence/pipeline.py), [`db/migrations/versions/0011_pipeline_record.py`](../../../db/migrations/versions/0011_pipeline_record.py), [`apps/web/legacy-frontend/src/app/components/PipelineFunnelTiles.tsx`](../../../apps/web/legacy-frontend/src/app/components/PipelineFunnelTiles.tsx) | Re-enter only with a CBA-defined post-event outcome and metric-register definition. Wave 5 may implement `confirmed` and `attended`, not `member_inquiry`. |
| Legacy cold outreach and specialist discovery | Do not mount, navigate to, or reuse on CBA paths. Preserve only the consented `/v1` outreach path. | [`apps/web/legacy-frontend/src/app/pages/Outreach.tsx`](../../../apps/web/legacy-frontend/src/app/pages/Outreach.tsx), [`apps/web/legacy-frontend/src/components/AgenticOutreachPanel.tsx`](../../../apps/web/legacy-frontend/src/components/AgenticOutreachPanel.tsx), legacy helpers in [`apps/web/legacy-frontend/src/lib/api.ts`](../../../apps/web/legacy-frontend/src/lib/api.ts); preserved path in [`services/api/smartmatch_api/routers/outreach.py`](../../../services/api/smartmatch_api/routers/outreach.py) and [`services/worker/smartmatch_worker/outreach.py`](../../../services/worker/smartmatch_worker/outreach.py) | Re-entry requires a future customer scope change. Consent eligibility in [`python/smartmatch_domain/smartmatch_domain/consent.py`](../../../python/smartmatch_domain/smartmatch_domain/consent.py) must never be weakened. |
| External event/speaker discovery, crawling, scraping, and paid extraction | Disable live execution and hide crawl controls. Fixture ingest remains usable for tests only; CBA records grow manually. | [`docs/decisions/g3-crawler-decision.md`](../../decisions/g3-crawler-decision.md), [`python/smartmatch_providers/smartmatch_providers/fixture_ingest.py`](../../../python/smartmatch_providers/smartmatch_providers/fixture_ingest.py), [`services/worker/smartmatch_worker/paid_extraction.py`](../../../services/worker/smartmatch_worker/paid_extraction.py), [`apps/web/legacy-frontend/src/components/CrawlerFeed.tsx`](../../../apps/web/legacy-frontend/src/components/CrawlerFeed.tsx) | Re-entry requires explicit customer authorization, signed threat model, and live-data/provider gates. Customer §20 currently prohibits it. |
| Landing-page scraping/CRM narrative | Replace or hide CBA-inaccurate claims; do not imply external acquisition. | [`apps/web/legacy-frontend/src/app/pages/LandingPage.tsx`](../../../apps/web/legacy-frontend/src/app/pages/LandingPage.tsx) | Re-enter copy only if it truthfully describes in-scope manual records and consented workflows. |
| CPP green/gold redesign | Defer. A small token-only change may be separately approved, but branding cannot block functionality. | [`apps/web/DESIGN.md`](../../../apps/web/DESIGN.md), legacy frontend theme assets | Re-enter after functional demo path and design Part 2 decisions; customer §21/P2. |
| Live Resend email | Keep fixture transport and fail-closed selection. | [`services/worker/smartmatch_worker/outreach.py`](../../../services/worker/smartmatch_worker/outreach.py), provider configuration referenced by [`services/api/smartmatch_api/config.py`](../../../services/api/smartmatch_api/config.py) | Explicit provider credentials, non-classroom edition, approved templates/domain, and user authorization. |
| Live JWKS/institutional identity | Preserve the single pilot login; do not add role selection or claim institutional SSO. | [`services/api/smartmatch_api/routers/auth.py`](../../../services/api/smartmatch_api/routers/auth.py), [`docs/decisions/pilot-login-decision-2026-09-04.md`](../../decisions/pilot-login-decision-2026-09-04.md) | A1b institutional configuration and explicit live-identity approval. |
| Terraform/cloud apply | Plan/validate only; no apply and no cloud-ready claim. | [`infra/terraform/`](../../../infra/terraform/), [`tools/env_isolation_check.py`](../../../tools/env_isolation_check.py) | Explicit user authorization with deployment target, credentials, and live gates. |

## Superseded IA-West execution goals

- Do not re-run G2, G3, or G4; their shipped work remains historical architecture, not CBA scope.
- G1 may continue only for `confirmed` and `attended`; no CBA `member_inquiry` writer.
- G5 and G6 live integrations remain deferred.
- `P-CRAWL-LIVE` remains gated off. Do not re-run superseded IA-West `P-REWARDS-*` goals as written; this is not a gate on the working rewards capability. A future CBA rewards card must preserve server-backed behavior and limit itself to approved P2 wording/refinements or a named truthfulness defect.
- G7, G8, and G9 are eligible for Wave 5 only when rewritten with the CBA fences in this register.

## Enforcement expectations for Wave 0

Planning assumes one explicit `PRODUCT_PHASE=cba`-style capability policy (exact representation to be chosen during implementation) shared by API composition and frontend navigation. Tests must prove gated CBA routes/links are absent or return a truthful disabled response, while preserved consented outreach, event reads, discovery feed, and one backend-derived login flow remain available.

`ALLOW_LIVE_PROVIDERS=false`, `ALLOW_LIVE_DATA=false`, and `ALLOW_CLOUD_DEPLOY=false` remain mandatory defaults. This document authorizes no code or environment change.
